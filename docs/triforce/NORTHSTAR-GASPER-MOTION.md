# GASPER MOTION NORTH STAR — NATURAL PHYSICS AND PERFORMANCE

**Status:** OPEN — seq18 closed one-membrane. Isolated
gait10 locked by Sol's 6s visual walking gate (N288). Full
20s evidence take (seq19) is now legal as context survival,
not acceptance (N289). Owner human acceptance is required
and never self-issued (S3).

**Version:** 2026-08-15 · `GASPER-MOTION-NORTHSTAR-001` / N295

**Lead residual:** N328 gait10 lobe walk on seq18.
Swing lift now paints the air gap. Not Cody
acceptance. No self-PASS.

**Purpose:** Replace the invalidated r11 motion closure with a narrow,
executable acceptance contract for movement that reads as physically
intelligent, intentional, and alive at source cadence.

## Current certified frontier — N152

The historical phase notes below remain as chronology; they are not the active
runtime contract. The current source, proof, and live-surface authority is:

- `main` contains the N152 source checkpoint `4fbf9375a`, clean reconciliation
  `b0f72c372`, canonical 5179 proof `c25794bd8`, and its recorded reconcile
  `5cb808a37`; the Tuning Lab Northstar is anchored on that canonical surface.
  The source delta is the bounded, support-driven Wispwalker mass exchange
  described below. Face law is now N178: eyes and mouth are apertures
  inside the membrane (docs/triforce/NORTHSTAR.md).
- Safety ref `capstone/pre-main-reconcile-2026-08-10` at
  `0cf58721a452e423915df792091ce1750ec6ac34`.
- Canonical live preview and browser-owned Tuning Lab: `http://127.0.0.1:5179/`
  and `http://127.0.0.1:5179/__gasper/tuning-lab`.
- Current user-preferred compound recipe: `verticalDepthGain: 0.85` with the
  bounded Wispwalker rehearsal (`footRootGain: 1.75`, `walkAmp: 1.3`,
  `walkAccent: 0.68`, `stepDepth: 7.6`, `walkPeriod: 1.15`,
  `footworkTempo: 1.08`, `actingGain: 1.16`, `viscoTau: 0.34`).
- Current canonical coupled proof:
  `research/proofs/gasper-physics-001/isolated-n152-canonical-5179-120fps/`.
- Source-backed validation witness retained for comparison:
  `research/proofs/gasper-physics-001/isolated-n152-source-support-step-northstar-live-5180-120fps/`.
- Fresh chronological review surface:
  `research/proofs/gasper-physics-001/isolated-n152-canonical-5179-120fps/active-review-contact-sheet.jpg`.
- The canonical 5179 preview serves the rebuilt N152 bundle and has now been
  captured directly; the N140/N141 pair remains historical comparison proof.

The exact-rate machine gates, full test suite, typecheck, production build,
TriForce doctor, and reconcile are green. The canonical N152 capture is now
source-backed on 5179 itself. The remaining gate is chronological
architect/owner visual acceptance of intentional weight transfer and acting
quality; it is not self-issued by this document.

## N151/N152 — physics-driven Wispwalker mass exchange

The next source correction removes the renderer's independent travel rhythm
from the moving Wispwalker mass. During physics-owned travel, the broad body
asymmetry, lean, and post-x carrier now use the same support conclusion as the
root lobe (`stepBaseXUnits`/`swayXUnits`, flatten carrier, or the bounded
depth-phase fallback). At a physics-owned hold, the authored walk expression
settles instead of continuing a clock-shaped in-place performance. Non-physics
walk-in-place authoring remains available, and `WorldPhysicsDriver` remains the
sole motion writer.

Canonical 5179 witness plus the retained validation comparison:

- N151 Wispwalker showcase: exact 120fps, `750` total / `600` active frames,
  focused machine gate green, gait runs `106 / 113` with `8` reversals, wind
  pass, foot-root `600/600`, and support-carrier pass.
- N152 coupled compound (`verticalDepthGain: 0.85` plus the Wispwalker
  rehearsal): exact 120fps, `1,350` total / `1,200` active frames, green
  focused gate, zero-delta handoff, gait runs `112 / 29` with `12` reversals,
  active vertical peak `367.63`, foot-root `600/600`, wind pass, and support
  carrier pass. The canonical 5179 receipt records `+4.2482px / -0.8327px`
  wind pressure response, above the declared `0.5px` threshold.

This is the current machine/live candidate. It is not yet an owner acceptance
claim; human review of the chronological take remains open.

## N140/N141 — support-aware root exchange

The latest source frontier keeps the broad authored Wispwalker mass, lean, and
travel scaffold intact while tightening only the root-exchange seam. The
in-place fallback subtracts its authored phase-origin load so a stopped
Wispwalker begins neutral. During real travel, only the paired foot-root lobe
uses the physics support conclusion (`stepBaseXUnits`/`swayXUnits`, then the
flatten carrier, then the pure-depth fallback); `WorldPhysicsDriver` remains
the sole physics writer and no second clock or motion authority was added.
This preserves the authored wind response while making the planted side hold
through stance and exchange in the support window.

Fresh witnesses from the unchanged live `5179` surface:

- N140 baseline: exact deterministic 120fps, `1,350` total / `1,200` active
  frames, focused machine gate green, wind pressure deltas `+10.8854px /
  -4.3482px`, support fraction `0.8739`, flatten-step sign `0.8544`,
  shadow-step sign `0.9817`, gait runs `55 / 114` with `7` reversals, and
  foot-root readability `600/600` with `7.366px` minimum cleft.
- N141 compound (`verticalDepthGain: 0.85` plus the bounded Wispwalker
  rehearsal): exact deterministic 120fps, `1,350` total / `1,200` active
  frames, focused machine gate green, wind pressure deltas `+8.6429px /
  -2.5778px`, support fraction `0.8792`, flatten-step sign `0.8029`,
  shadow-step sign `0.9868`, gait runs `282 / 15` with `6` reversals, active
  vertical peak `368.07`, and foot-root readability `600/600` with `6.009px`
  minimum cleft.

The source-level neutral/support law has static regression coverage, and the
full suite is green at `55` files / `776` tests. The N141 capture is a machine
and live proof plus a conditional architect review surface; owner acceptance of
the acting quality remains open.

## N128/N132 — support-exchange timing closure

The coupled route exposed one real current-source mismatch: the renderer-facing
flatten patch could lag the physics step sign during a planted-side exchange.
The source correction is deliberately expression-side only. On a sign crossing,
the existing Z3 flatten lag receives a bounded `×4` response and a per-frame
`11.5`-unit delta cap; steady-state amplitude, width lag, support authority,
and the sole `WorldPhysicsDriver` writer remain unchanged. A focused regression
requires the visible flatten sign to agree with the planted step through a
five-leg exchange and forbids a large one-frame flatten jump.

Fresh live witnesses from the rebuilt production bundle and the unchanged 5179
preview:

- Baseline N131: exact deterministic 120fps, `1,350` total / `1,200` active
  frames, zero-delta handoff, Wispwalker/Presence/Boo order, wind/gait/
  foot-root/body/output/projection locks green, support fraction `0.8739`,
  flatten-step sign agreement `0.8544`, shadow-step agreement `0.9817`, and
  focused machine gate green.
- Compound N132: exact deterministic 120fps, `1,350` total / `1,200` active
  frames, `verticalDepthGain: 0.85` with the complete Wispwalker rehearsal,
  active vertical peak `368.36`, representative cleft `6.42px`, foot spread
  `49.97px`, flatten-step sign agreement `0.8029`, shadow-step agreement
  `0.9868`, and focused machine gate green.

The N132 contact sheet and 120fps masters are the current review surface. This
is a machine/live proof and a conditional architect visual pass; owner human
acceptance of the final acting quality remains open.

## N123/N125 — short-leg arrival continuity

The remaining live seam was a two-cap acceleration exchange when the Coulomb
brake crossed its switching surface on the compact authored showcase legs. The
source correction is deliberately narrow: a 20ms gain floor applies only inside
the 220-unit arrival corridor when the authored leg began within 500 units;
long arrivals and the existing reversal law remain unchanged. The focused
regression mirrors the five-leg showcase path, requires the full 120fps jerk
envelope to stay within one traction cap, and requires every endpoint to settle
within one world unit.

The current live 5179 receipt is the N124 capture above: exact 120fps, 750 total
and 600 active samples, focused machine gate green, bidirectional travel, green
wind response, 100% foot-root readability, and support-carrier coherence
(0.7978 fraction; 0.8069 flatten sign agreement). The companion MP4 and
chronological contact sheet in that directory are the review artifacts. This
is a machine/live proof, not owner acceptance.

## North Star sentence

Gasper must read as a vector organism with mass, intention, and memory: every
movement carries naturally from the previous physical state, Wispwalker walks
with grounded weight transfer, and every footless embodiment floats with
bounded flight, believable anticipation, drag, settle, facing, and expression.

## Non-negotiable laws

- `WorldPhysicsDriver` remains the sole free-motion writer; renderers project
  its state and never replace it.
- Wispwalker is the only walking embodiment. Footless embodiments float.
- The organism clock is the only time source. Rendering remains vector-only;
  no ambient `Math.random`, no pupils, no jump ring, and the near-glass bound
  remains intact.
- Every review master is captured headlessly at exactly 120fps and checked
  with `ffprobe`. A still, contact sheet, 30fps derivative, or machine receipt
  cannot close a motion phase alone.
- The existing working tree is protected from destructive reset/clean/stash/
  rebase/merge/branch-switch operations; validated, explicitly authorized
  Northstar checkpoints may be committed without self-issuing owner acceptance.
- The historical 5174/5176 lanes are retired. The canonical live preview and
  capture surface is the 5179 Gasper Studio server; it is not restarted during
  a review without explicit authorization.
- A phase advances only after machine gates, live/product evidence, a fresh
  chronological video, and architect visual review all return to chat.

## Baseline truth — r11 is reopened

The previous coupled receipt proved per-frame identity and embodiment order,
but it did not prove continuity between frames. Fresh inspection of
`research/proofs/gasper-physics-001/northstar-r11-northstar-demo-surface-r3/samples.json`
found:

- At the walk-to-flight handoff, capture frame `749 → 750` moves the body from
  `x = 0` to `x = -700` in one 120fps step. This is a teleport, not a physical
  transition.
- The Boo carrier changes from `0` to its full `-10` unit clamp on the same
  handoff frame because Boo activation has no expression ramp.
- The later comet release contains a `226.8` world-unit single-frame move and
  extreme discrete acceleration spikes. The hard impulse is valid as a
  standalone shot, but it is not yet a natural continuation of a visible walk.
- Wispwalker gait expression begins abruptly at capture start; the largest
  observed gait-bob derivative is about `20.26` world units per frame, with
  step/root discontinuities that make the walk read mechanically.

The previous body/output residual of `0` is not contradictory: the reset made
the body and emitted pose agree after the teleport. That gate was measuring
authority identity, not visual continuity. The acceptance contract is therefore
reopened rather than silently weakened.

## Fresh r6 audit — Phase 1 is not full flight closure

The handoff/release repair is real, but the fresh exact-120fps r6 proof exposed
the next defects that this North Star must now own:

- `comet-fly` still selected an empty physics input, so the release entered the
  legacy gravity/impulse path instead of the footless thrust/drag/hover law.
  The measured in-flight displacement reached `63.49` world units in one
  120fps sample and the travel tilt reached its `20°` legacy cap.
- Automatic retirement still called `disarm()` while a footless body was at
  its hover/home altitude. That hard reset produced a `192.69` world-unit
  vertical discontinuity and a `4.61`-unit Boo-expression drop on the final
  `locomotion → idle` frame.
- Wispwalker still enters gait abruptly: the r6 gait-bob derivative peaks at
  `20.26` world units per sample. The gait receipt passes motion presence, but
  not natural entry timing.
- Coupled r6 facing reached bounded display yaw, but `faceFloorExercised` was
  `false`; no face or 3⁄4 sign-off may be inferred from that run.

Therefore `bodyOutputResidualMax = 0` remains necessary but explicitly does
not close flight trajectory, retirement continuity, gait entry, or facing.

## Fresh r8 repair receipt — Phase 2 mechanical sub-gate

The next red regressions were repaired in the active `WorldPhysicsDriver` seam:

- `comet-fly` now selects `flightInput()` and therefore remains inside the
  footless thrust/drag/hover law. The legacy gravity/impulse fallback is no
  longer the release path.
- Automatic performance retirement now enters a clock-driven `retiring` mode,
  reuses horizontal flight control while lowering the hover target to the
  floor, and calls `disarm()` only after the body is neutral. Explicit
  `disarm()` remains an immediate release API; the performance path no longer
  uses it as a hidden teleport.
- The two regressions are covered by the driver suite: footless comet flight
  rises under the hover law, and a spent airborne flight reaches neutral before
  returning to `idle`.

Fresh evidence at
`research/proofs/gasper-physics-001/northstar-motion-phase2-flight-r8/` reports
`70/70` driver tests, `5/5` Boo-law tests, exact deterministic `120fps`,
`1200` active samples, `focusedMachineGate: true`, `bodyOutputResidualMax: 0`,
`activeMaxAbsTilt = 8.16`, `floatGaitCollapsed: true`, and a `120/1` H.264
master. The focused review artifact is
`phase2-handoff-flight-retirement-120fps.mp4`: 324 chronological frames,
`1280×900`, and `120/1` by `ffprobe`.

Adjacent transition telemetry now reads as a physical sequence rather than a
reset: `comet-gather → comet-fly` has `dx=0.1068`, `dy=0.6751`; the later
`comet-fly → locomotion` handoff has `dx=-1.682`, `dy=0.1673`; and the final
`retiring → idle` frame has `dx=-0.0788`, `dy=0`, with zero velocity and ground
contact. The largest in-flight adjacent deltas remain `|dx|=28.7766` and
`|dy|=24.0769`, so flight speed/arc quality is machine-described but not yet a
full coupled-performance sign-off.

**Architect visual review:** **CONDITIONAL PASS for the Phase 2 mechanical
sub-gate.** Chronological contact sheets and selected frames show no handoff
teleport, bounded flight tilt, and a visible neutral retirement. This is not a
G3/G5 closure: the proof is a surface capture, `faceFloorExercised` is still
`false`, Wispwalker entry still peaks at about `20.26` world units per sample,
and face/yaw/3⁄4 quality has not been isolated and signed off. Owner human
acceptance remains separate.

Capture-side 404/connection-refused console events are also present in the r8
receipt. They did not invalidate the focused machine gate, but they remain an
environmental risk to clear before the final coupled acceptance run.

## Fresh r12 repair receipt — Phase 3 gait entry/lean sub-gate

The Wispwalker seam now has one declared temporal profile instead of inserting
the full visible gait expression on the first walking sample:

- `GaitLaw.ts` declares a `180ms` expression ramp: three gait-law critical
  windows. `WorldPhysicsDriver` gates bob, sway, roll, contact squash, planted
  base, and walking bank through that gate.
- Grounded lean is acceleration-derived and low-passed with the existing bank
  time constant. It is no longer cut to zero when speed crosses the old
  epsilon, so cruise entry and final floor settle cannot create a one-frame
  lean snap.
- The isolated proof recipe now travels between bounded `±280` targets. The
  prior `±900` recipe reached the production corridor wall and contaminated the
  gait receipt with repeated wall corrections; this capture is the narrow
  entry/reversal/settle surface.

The red-to-green evidence is explicit: the first pre-ramp gait sample was
`5.536` bob units, the cruise lean discontinuity was `5.725753°`, and the
speed-epsilon settle discontinuity was `3.38429°`. The final focused driver
suite is `73/73`, `npm run build` passes, and the r12 receipt at
`research/proofs/gasper-physics-001/northstar-motion-phase3-gait-r12/` reports
exact deterministic `120fps`, `690` frames with `540` active samples,
`focusedMachineGate: true`, `activeMaxAbsTilt = 5.83`, grounded locomotion,
`wispwalkerGaitMotion: pass`, `footRootReadability: pass` with coverage `1`,
and `faceFloorExercised: false`.

The two focused artifacts are `gait-entry-120fps.mp4` and
`gait-turn-settle-120fps.mp4` in that same directory. `ffprobe` verifies each
as `72` frames at `120/1`. Chronological review of the entry and the authored
turn/settle interval, including captures `419 → 421` and `440 → 441`, shows
continuous lower contact, no body teleport, and no one-frame face
disappearance. The maximum adjacent pose-tilt change in r12 is `1.5581483°`.

This is a **conditional architect visual pass for gait entry and lean
continuity**, not a G2 closure. The receipt's `travelReversalCount = 16` is
currently noisy because it counts near-zero sign crossings while the body is
settling at the authored target; the physical reversal itself is visible at
capture `420` (`x ≈ 280.17 → 279.19`, `vx ≈ 59.64 → -176.57`). A dedicated
near-rest stability metric and a clean authored brake/settle receipt are still
required before calling Wispwalker fully machine-proven. Owner human
acceptance remains separate.

## Fresh r20 repair receipt — Phase 3 rendered gait/arrival sub-gate

The near-rest seam is now measured against the rendered contour rather than a
local path witness, and the proof route no longer lets depth perspective carry
the shot. The arrival-band regression is explicit: the old one-unit velocity
floor produced a `83.034460` tail-speed chatter value; the repaired controller
holds the authored target with a zero tail-speed envelope. The receipt also
counts only sustained direction changes, so target-settle noise cannot
masquerade as authored reversals.

The focused driver suite is now `74/74`, including the arrival-band regression;
the proof-metric suite is `12/12`, and the production build passes. The clean
subject-close r20 receipt at
`research/proofs/gasper-physics-001/northstar-motion-phase3-gait-r20/` reports
exact deterministic `120fps`, `750` total frames with `600` active samples,
`focusedMachineGate: true`, `activeXSpan = 557.28`, `activeZSpan = 207.35`,
`travelReversalCount = 4`, `activeMaxAbsTilt = 5.59°`, zero body/output and
projection residuals within the declared pixel lock, face visibility `1`, and
bounded display yaw `44.12°`. Rendered foot-root readability covers `93.5%`
of active samples; the machine gate passes, with minimum observed spread
`44.04px` and representative cleft depth `5.23px`. The gait exchange metric
passes with eight sustained reversals and a positive/negative support run.

The focused chronological artifacts are `gait-entry-120fps.mp4`,
`gait-corner-120fps.mp4`, and `gait-final-settle-120fps.mp4`; each is `72`
frames at `640×640`, encoded and verified at `120/1`. Architect review of the
selected entry, corner, and settle intervals finds no teleport, face
disappearance, or one-frame tilt reset, and the shallow-depth route keeps the
walk readable instead of turning it into a scale demo. This is a **conditional
architect visual pass for the Phase 3 gait/arrival sub-gate**, not owner
acceptance and not a G4/G5 closure.

The earlier r19 surface capture is diagnostic only because its review sheet
included the studio UI; r20 is the clean visual evidence. Face-floor exercise,
active-path yaw/3⁄4 sign-off, the dedicated Boo/float proof, console cleanup,
and the final coupled performance remain open.

## Fresh r1 repair receipt — Phase 4 facing/2.5D sub-gate

The repaired facing contracts are green against the active renderer path: the
radial-facing, render-contract, physics-silhouette, and embodiment-authority
suites pass `31/31`. The isolated full-yaw receipt at
`research/proofs/gasper-physics-001/northstar-motion-phase4-facing-r1/`
is exact deterministic `120fps`, has `focusedMachineGate: true`, exercises the
face-floor case with minimum face visibility `0.72`, reaches maximum display
yaw `179.51°`, retains a 3⁄4 width cue, and holds both equation and pixel
projection locks. The isolated 3⁄4 receipt at
`northstar-motion-phase4-threequarter-r1/` is also exact `120fps` and
machine-green: minimum face visibility `1`, maximum display yaw `44.95°`,
3⁄4 width cue observed, and both projection locks intact.

Chronological frame review finds no face disappearance, contour blow-up, or
flat-card collapse. The 3⁄4 case keeps a readable face and side-thickness cue;
the full-yaw case keeps a bounded face presence through the floor exercise.
This is a **conditional architect visual pass for the Phase 4 facing/2.5D
sub-gate**, not owner acceptance: the isolated facing recipes prove the
projection contract, while the active r20 travel path proves only bounded
travel-derived yaw (`44.12°`) and does not yet exercise the face floor. The
dedicated Boo/float proof, clean proof-surface console state, and final coupled
performance remain open.

## Fresh r2b/r1 repair receipt — Phase 2/3 float sub-gate

The clean subject-close Boo exit recipe at
`research/proofs/gasper-physics-001/northstar-motion-phase4-boo-r2b/` is exact
deterministic `120fps` with `540` active samples. Its machine receipt is green:
the active sequence observes `presence`, Boo mode, airborne altitude, zero
gait expression, face visibility `1`, bounded display yaw `44.99°`, body/output
continuity, and both projection locks. The authored state sequence reaches
`comet-fly → locomotion → retiring → idle`; chronological review of entry,
peak flight, retirement, and neutral frames finds no gait leakage, face loss,
teleport, contour blow-up, or retirement snap.

The companion Presence showcase at
`research/proofs/gasper-physics-001/northstar-motion-phase4-presence-r1/` is
also exact deterministic `120fps` and machine-green: `600` active samples,
four sustained travel reversals, no grounded locomotion, zero gait, face
visibility `1`, projection locks, and an admitted wind-response witness with
positive and negative directional runs. Its subject-close review stays
buoyant and readable through the authored turns.

This is a **conditional architect visual/machine pass for the float sub-gate**,
not owner acceptance. The earlier r8 receipt remains the flight-law mechanical
receipt. At that point in the chronology, the coupled performance and clean
proof-surface console state were still open; the r2 handoff repair and r3
capture-surface candidate below supersede that residual.

## Fresh r2 repair receipt — Phase 5 coupled handoff sub-gate

The repaired coupled master at
`research/proofs/gasper-physics-001/northstar-motion-phase5-final-r2/` is exact
deterministic `120fps`, with `1,200` active samples at `640×640` and a green
focused machine gate. The receipt proves Wispwalker-first/Presence-second
ordering, grounded gait and rendered foot-root coverage, zero gait in Presence,
wind response, Boo mode, altitude, face visibility `1`, bounded display yaw
`45°`, body/output equality, and equation/pixel projection locks.

The new handoff witness is adjacent and explicit:
`northstarHandoff.pass: true`, `fromMode: locomotion`,
`toMode: comet-gather`, and position delta `0`. The prior coupled recipe's
idle/origin reset was rejected by the red regression and removed from the
showcase path. Chronological review of the handoff (`749 → 750`), first flight
rise, peak, and retirement-to-neutral intervals finds continuous face presence,
no body teleport, no foot-root snap, no projection blow-up, and no visible
retirement discontinuity.

This is a **conditional architect visual/machine PASS for the coupled motion
gate**, not owner acceptance. The r2 receipt remains the historical red-to-
green handoff repair witness; r3 below is the current clean proof-surface
candidate.

## Fresh r3 receipt — clean coupled proof surface

The r3 capture at
`research/proofs/gasper-physics-001/northstar-motion-phase5-final-r3/` keeps
the r2 motion recipe unchanged and adds only a capture-scoped explicit offline
response for the optional AgentBridge token probes. This prevents an absent
optional bridge from producing browser 404/connection-refused noise in a
standalone physics proof; it does not claim that a live AgentBridge connection
was exercised or self-accepted.

The fresh receipt is exact deterministic `120fps`, with `1,200` active samples,
`activeMp4Probe.r_frame_rate: 120/1`, `events: []`, and
`offlineBridgeStub: true`. The coupled machine gate remains green: handoff
`locomotion → comet-gather` has position delta `0`, face visibility is `1`,
display yaw is bounded to `45°`, gait/float separation, wind response,
foot-root readability, body/output equality, and projection/pixel locks all
pass. Chronological review of the gait, adjacent handoff, flight, and
retirement intervals found no face loss, teleport, yaw flip, gait leakage, or
retirement snap.

This is the **current conditional architect visual/machine PASS** for the
coupled motion and proof-surface gates. Owner acceptance remains pending; the
live optional bridge path is a separate operational audit, not a physics
acceptance dependency.

## Fresh r4 receipt — live standalone proof

The r4 capture at
`research/proofs/gasper-physics-001/northstar-motion-phase5-final-r4/` is the
current coupled master. It runs with `offlineBridgeStub: false`, after the
product-side standalone policy was rebuilt and audited. Ordinary browser/CDP
previews no longer auto-start the optional AgentBridge client; packaged/Tauri
launches still do, and browser previews can explicitly opt in through the
bridge autostart global when that integration is under test.

The live browser audit recorded zero console errors, zero failed requests, and
zero HTTP errors. The durable audit receipt is
`research/proofs/gasper-physics-001/northstar-motion-phase5-final-r4/live-browser-audit.json`.
The fresh exact deterministic `120fps` receipt has `1,200`
active samples, `events: []`, a green focused machine gate, and the same
zero-delta `locomotion → comet-gather` handoff. The r4 chronological review
again finds no face loss, teleport, yaw flip, gait leakage, projection blow-up,
or retirement snap.

The supplementary full-surface r5 witness at
`research/proofs/gasper-physics-001/northstar-motion-phase5-final-r5/` confirms
the same exact-`120fps` machine gate and approximately `949.48` world-unit
active X travel at `1280×900`. It is retained as a spatial audit; r4 remains
the sign-off artifact because its subject-close framing removes Studio chrome
and keeps the movement contour legible.

This is the **current conditional architect visual/machine PASS** for the
coupled motion and live standalone proof-surface gates. Owner acceptance
remains pending; live AgentBridge connectivity is now an explicit packaged or
opt-in browser capability rather than an accidental preview dependency.

## Fresh r21 receipt — current support-carrier Wispwalker witness

The current production source was re-captured with the formal
`capture-isolated-beat-120fps.mjs wispwalker-showcase` recipe at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-current-support\`.
The receipt is exact deterministic `120fps` with `750` total frames and `600`
active samples. Its focused machine gate is green: Wispwalker remains the
rendered profile, grounded locomotion and gait expression are observed, the
bounded showcase records four sustained reversals and `559.9` units of active
screen travel, body/output residual is `0`, and both the equation and pixel
projection locks pass. The current support-carrier result raises rendered
foot-root coverage to `97.17%` (`583/600` samples), with a representative
`6.09px` center cleft and `48.43px` foot spread.

The exact active clip is
`wispwalker-120-current-support/isolated-wispwalker-showcase-active-120fps.mp4`;
the adjacent `active-review-contact-sheet.jpg` is the compact visual review
surface. This is a **current conditional architect machine/visual pass** for
the support-carrier packet. The shell intentionally remains mass-continuous,
so the owner-facing “unmistakable intentional step” judgment is still open;
do not convert this receipt into human acceptance or a global North Star
close.

## Fresh r22 receipt — current coupled handoff witness

The same current source was then run through the exact deterministic
`northstar-demo` recipe at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-current-support\`.
The receipt records `1,350` total frames and `1,200` active samples at
`120fps`, with a green focused machine gate. The Wispwalker-first/Presence-
second order is observed; the midpoint handoff remains
`locomotion → comet-gather` with `positionDelta: 0`; grounded gait, footless
altitude, zero Presence gait, Boo mode, bounded `45°` display yaw, wind
response, body/output equality, and both projection locks all pass. The
coupled Wispwalker half retains `96.83%` (`581/600`) foot-root coverage.

The current active clip is
`northstar-demo-120-current-support/isolated-northstar-demo-active-120fps.mp4`
with `active-review-contact-sheet.jpg` beside it. This is the current-source
conditional architect/machine pass for the coupled handoff; r4 remains the
live-browser-audit sign-off artifact. Owner acceptance of the acting and
coupled-performance gates remains open.

## Fresh r23/r24 receipts — crisp-foot restoration

The archived cusp investigation gave the next bounded hypothesis: restore the
pre-soften width of the load-bearing foot roots because the shipped
viscoelastic contour inertia—not the widened foot geometry—owns the old
high-frequency jitter fix. The current source now uses `sigma: 0.13` for the
two primary Wispwalker foot lobes and the two structural foot nub lobes, while
the center cleft remains softened at `0.155`. This is a contour-shape-only
correction; the physics driver, clock, gait carriers, and topology are
unchanged.

The isolated receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-crisp-foot-r1\`
is exact deterministic `120fps`, with a green focused machine gate and
`footRootReadability.coverage = 1.0` (`600/600`). Minimum cleft depth is
`4.31px`, representative spread is `52.85px`, and gait motion, reversals,
grounded contact, body/output equality, and both projection locks pass.

The coupled receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-crisp-foot-r1\`
is also exact `120fps` and machine-green. The Wispwalker → Presence/Boo
handoff remains zero-delta and ordered, Presence stays gaitless and airborne,
and the coupled Wispwalker half retains `100%` foot-root coverage with a
`5.11px` minimum cleft. Chronological review shows a cleaner two-root lower
contour without a snap or detached layer. This is the current conditional
architect winner for the G2 structural foot read; human acting acceptance and
the final G5 performance verdict remain open.

## Fresh composition/facing refresh — current-source continuity

The post-N44 composition packet is now reconciled into the running Northstar.
The production witness at
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\ns1-t2\`
ran for 240 seconds at 65.78 fps. Its far-presence envelope holds the organism
above `φ⁻¹ = 0.618` apparent scale, the stored production camera stays at
zoom `2.2564` with zero within-run reframes, and the final-frame composition
observer passes `4,794/4,794` samples. This is the current N44 composition
machine slice; it does not self-issue human visual acceptance.

The facing contract was then refreshed against the crisp-foot source. The
manual-yaw witness at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\manual-yaw-face-120-crisp-foot-r1\`
is exact `120fps`, reaches `179.51°`, exercises the face floor and rear-face
suppression, and keeps both projection locks green. The three-quarter witness
at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\three-quarter-face-120-crisp-foot-r1\`
is also exact `120fps`, reaches `44.95°`, retains face/side thickness, and
keeps both projection locks green. The renderer’s crisp-foot correction does
not reopen G4.

The open performance residual is therefore narrow: owner-facing judgment of
Wispwalker acting and the final coupled G5 read, plus an architect decision on
whether a true active-organism camera reframe should ease. No completion or
integration claim is issued from machine receipts alone.

## Fresh r25 receipt — envelope-safe coupled performance

The coupled proof route itself had one evidence defect: its final walking
targets extended past the live composition wall, allowing wall restitution to
look like an authored step reversal. The route now stays inside the production
envelope while preserving the same live-body handoff. The fresh receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-envelope-r2\receipt.json`
is exact `120fps`, with `1,200` active samples, a green focused machine gate,
zero-delta `locomotion → comet-gather`, six sustained travel reversals, no
active collision events, and `100%` Wispwalker foot-root coverage. The active
travel span is `778.59` units and max tilt is `6.04°`; this is a proof-route
correction, not a physics-authority change. Chronological review of the fresh
contact sheet shows the coupled Wispwalker half without the previous boundary
collision contaminant.

## Fresh r26/r27 receipts — locomotion-dwell refractory gait

The next canon audit found a genuine state seam rather than a renderer preference:
the visible gait gate was keyed to locomotion mode, so a driver that remained in
locomotion while dwelling on a settled target kept the step expression fully open.
The first relaunch then inserted the full vault instead of entering through the
180 ms perceptual ramp. A red-first regression failed `77/78` before the source
change and passes `78/78` after `WorldPhysicsDriver.ts` requires grounded speed
above `GAIT_LAW.speedEpsilonUnitsPerSec` before opening the expression gate. The
body law, travel-integrated phase, topology, and authority ownership are
unchanged; only the visible expression's dwell/re-entry state is corrected.

The fresh isolated receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-refractory-r1\`
is exact deterministic `120fps` (`750` total, `600` active) with a green focused
gate, grounded gait, four reversals, body/output residual `0`, and both
projection locks. It records `559.9` units of travel and `4.58°` maximum tilt.
The foot-root witness is `598/600` (`99.67%`) with a `3.47px` minimum cleft; this
is retained as diagnostic evidence for the two borderline entry samples, not as
an owner-acceptance claim. The active clip is
`isolated-wispwalker-showcase-active-120fps.mp4` and the adjacent
`active-review-contact-sheet.jpg`.

The fresh coupled receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-refractory-r1\`
is exact deterministic `120fps` (`1,350` total, `1,200` active) with the focused
gate green. The zero-delta locomotion → comet-gather handoff, Wispwalker-first /
Presence-second ordering, airborne gait collapse, Boo mode, six sustained
reversals, zero active collision events, `100%` coupled foot-root coverage, and
both projection locks all hold; minimum cleft is `5.61px` and maximum tilt is
`6.07°`. Chronological review of the current contact sheet shows the route-safe
coupled motion without the prior boundary-collision contaminant. This is the
current machine/conditional-architect winner, while owner acceptance of acting
and final performance remains open.

## Rejected A/B — structural-root contact anchor

The proof loop tested one bounded alternative to the current contact-flatten
carrier: move the dent center from the inherited inward plant anchors
`θ=1.34/1.80` to the structural foot-root centers `θ=1.27/1.87`. The hypothesis
was that the support patch would read more directly on the load-bearing roots.
The source contract was red before the A/B and green after the candidate edit,
but the fresh exact `120fps` witness at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-root-anchor-r1\`
failed the actual visual/contour bar: foot-root coverage fell to `421/600`
(`70.17%`), minimum cleft fell to `1.04px`, and the contact sheet showed a
shallower, more lopsided lower mass. The candidate is reverted; the prior
`1.34/1.80` carrier remains the current source law. This failed attempt is
recorded to prevent repeating an amplitude/anchor knob without new evidence.

## Restored-source coupled witness

The restored current source was recaptured through the full coupled route at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-refractory-r2\`.
The receipt is exact deterministic `120fps` with `1,350` total and `1,200`
active samples and a green focused machine gate. The handoff is zero-delta;
Wispwalker and Presence remain correctly ordered; Presence is airborne and
gaitless; Boo mode, wind response, gait motion, six sustained reversals, 100%
foot-root coverage, no active collision events, and both projection locks are
observed. The active travel spans are `778.57` X and `482.57` Z, maximum tilt is
`6.03°`, and minimum coupled cleft is `5.83px`. The adjacent
`active-review-contact-sheet.jpg` and
`isolated-northstar-demo-active-120fps.mp4` are the chronological visual proof
surface. This refresh confirms the reverted source law; it does not convert
conditional architect review into owner acceptance.

## Bounded coupled flight witness — false-wall reversal removed

The prior restored-source coupled samples contained a hidden boundary event in
the Boo half: at frame `926`, the body reached the measured right wall and its
velocity changed from `+741.79` to `-360.65` in one fixed step. Because the
capture receipt did not carry wall events, that restitution was incorrectly
available to the reversal metric as if it were authored flight acting.

The proof route now files an explicit `vx: 1200` on the live-body comet
handoff. A red-first route contract in
`scripts/gasper-physics-001/proof-metrics.test.ts` fails against the old default
release and passes against the bounded route. The fresh exact-rate witness at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-flight-bound-r1\`
keeps the focused gate green, preserves the zero-delta handoff and all
Wispwalker/Presence/Boo ordering, and has no positive-to-negative velocity
change during `comet-fly`. The coupled spans are `599.93` X and `480.93` Z;
maximum tilt is `6.08°`, minimum Wispwalker cleft is `5.69px`, foot-root
coverage is `100%`, and both projection locks remain green. This preserves the
physics wall law for authored impacts while removing an accidental boundary
collision from the Northstar showcase evidence.

## Flight-to-home seam audit — retained

The bounded witness was also inspected at the spent-flight transition. Frames
`971 → 972` carry the live body with `Δx = 1.13` and `Δy = 0.40`; there is no
pose reset, hidden spawn, or wall restitution. Presence remains gaitless while
airborne (`stepHz = 0`), Boo’s bob stays continuous through the home turn, and
the body enters `retiring` only after reaching the home band. The largest
diagnostic home-turn velocity change is `7,998 u/s²`; it is produced by the
active flight force/drag response and does not insert a separate impulse. The
seam is retained as conditionally clean; reopen it only with a new
chronological visual defect, not from the mode label alone.

## Wispwalker contour cadence audit — retained

The same exact-rate bounded witness was inspected frame-by-frame for finite
lower-contour jumps, pose steps, and support-root snaps. The largest finite
contour delta is `30.70px` at capture `620`; it lands during a deliberate
`26.67`-unit travel step while gait bob/flatten eases. Adjacent frames remain
spatially continuous in the reviewed contact sheet, with no planted-root snap
or detached layer. Foot-root coverage is `100%` and the reported minimum
cleft is `5.69px`. This seam is retained; do not add a renderer-amplitude
control without a new chronological visual contradiction.

## Fresh r2 receipt — isolated Wispwalker current-law witness

The live production source was re-captured with the exact deterministic
`wispwalker-showcase` recipe at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-northstar-r2\`.
The receipt records `750` total and `600` active samples at `120fps`; the
focused machine gate is green with grounded gait, four reversals, wind
response, zero body/output residual, both projection locks, `100%` foot-root
coverage, and a `6.56px` minimum cleft. The active clip and contact sheet are
the current chronological visual surface. Review finds the same continuous
shell with a support-shaped base and no new contour snap, teleport, or gait
leakage. This is conditional architect evidence only; the owner-facing
intentional-step gate remains open.

## Fresh S0 support-hold receipt — retained

The current source applies the S0 planted-support law at the contour seam. A
red-first source contract covers the zero-screen-projection case: during real
locomotion the planted side is taken from the physics support carrier, and a
pure lateral walk uses the same `k = 2φ²` phase exchange as the kernel instead
of returning to a continuously sliding authored sine. The exact deterministic
live witness is at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-support-hold-r1\`.
It contains `750` total / `600` active samples at `120fps`, four sustained
travel reversals, wind response, `100%` foot-root coverage, a `5.37px` minimum
cleft, `586/600` active gait samples, zero body/output residual, both
projection locks, and no runtime events. The contact sheet and adjacent
frames show a continuous shell with a more persistent asymmetric support base;
there is no observed contour snap, detached layer, or gait leakage.
For owner-facing inspection, the same active clip is available as a stage-only
crop at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-support-hold-r1\wispwalker-support-hold-stage-crop-120fps.mp4`
with its chronological contact sheet beside it.

This is machine-proven and live-observed with conditional architect review.
The owner-facing G2 question — whether the exchange reads unmistakably as an
intentional step rather than a shell glide — remains open.

## Rejected low-projection threshold probe

The next narrow hypothesis promoted the S0 fallback whenever the projected
support carrier fell below `0.25` of sway. This was intended to rescue nearly
lateral travel whose screen projection was too small to read, without changing
the phase, amplitude, topology, or authority boundary. The red-first source
contract passed and the fresh exact deterministic witness is at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-support-threshold-r1\`.
It stayed machine-green with `100%` foot-root coverage, but its `5.07px`
minimum cleft was below the retained support-hold take's `5.37px`, and the
cropped chronological review did not show a meaningful visual improvement.
The threshold is reverted; it is diagnostic evidence only and is not part of
the current law.

## Axis diagnostic — support law visible when projection is present

The retained N107 source was also run through a depth-dominant diagnostic route
so the physical planted-support carrier projects into screen x instead of being
mostly edge-on. The exact deterministic witness at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-side-support-r1\`
is machine-green: `600` active samples at `120fps`, `559.9` Z travel,
`207.96` X travel, four reversals, zero body/output residual, both projection
locks, `100%` foot-root coverage, `5.68px` minimum cleft, and a passing gait
exchange with `+13.58px/-19.25px` directional peaks. The stage-only clip and
contact sheets show the alternating cyan root exchange more clearly than the
shallow lateral route, while the body remains one continuous shell.

This is a diagnostic separation, not a new source law: the physics carrier is
present and visually observable when the projection is favorable; the remaining
G2/G5 residual is the owner-facing legibility of that exchange in the product
route. The active clip is
`wispwalker-120-side-support-r1/isolated-wispwalker-showcase-active-120fps.mp4`;
the stage-only inspection clip is
`wispwalker-120-side-support-r1/wispwalker-side-support-stage-crop-120fps.mp4`.
The temporary capture-only path override was removed after the witness.

## Cycle-5 observer correction and current bar recomputation

The first 20-second observer-only run against the live preview did not enter a
walking segment, so its Cycle-4/5 result is an insufficient witness, not a
physics failure. The observer now reads `body.gait.speedRatio` from the
authoritative body-state packet, with the DOM `gaitSpeedRatio` attribute kept as
a legacy fallback. Recomputing the retained exact-rate side-on sample set at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-side-support-r1\samples.json`
produces `45` committed walking samples: normalized planted-base hold `0.9999`,
exchange fraction `0.20` inside the `0.10–0.26` fence, both planted sides
`+37.61/-37.40`, step/contact lock `0.196rad`, squash/contact lock `0.056rad`,
squash resultant `0.508`, skew maximum `1.933deg`, and normalized support delta
`0.6189` inside the `0.50–0.72` gate. This is a stronger current
machine/conditional-architect physics receipt, not an owner acceptance claim:
the body remains one continuous mass and the G2/G5 intentional-acting read is
still open. The durable recomputation receipt is
`research/proofs/gasper-physics-001/reviews/cycle5-current-side-support-recompute.json`.

## Acceptance gates

| Gate | Required behavior | Evidence that can close it |
|---|---|---|
| G0 · Governance | Current TriForce engine, CLEAN/BASELINE reconcile, one named residual, protected dirty tree | Boot/status/doctor receipt and paths on disk |
| G1 · State continuity | Position, velocity, depth, heading, and physical expression inherit across a mode handoff; no unannounced teleport or hard state reset | Adjacent-frame telemetry with transition IDs, predicted-vs-observed continuity, and a 120fps suspect-interval clip |
| G2 · Wispwalker | Feet remain grounded while walking; stance exchange, step placement, anticipation, braking, and settle read as weight rather than oscillation | Isolated Wispwalker clip plus gait/contact/derivative receipt |
| G3 · Float/flight | Footless forms never receive gait; Boo bob enters and exits through a ramp; comet release and retirement use coherent thrust, drag, hover, wind, and settle laws | Isolated Boo/Presence clips plus no-gait, bob-ramp, thrust/drag, retirement, impulse, and altitude receipts |
| G4 · Facing/2.5D | Radial travel direction drives bounded yaw; face never disappears; the shell retains a readable 3/4 thickness | Chronological yaw/3⁄4 clip at 120fps plus face/facing/depth receipt |
| G5 · Coupled performance | The complete sequence is impressive, legible, and free of visible jitter at source cadence | Fresh coupled 120fps master, contact sheet, frame-by-frame review, architect verdict |

## Continuity rules to encode before visual sign-off

1. **No hidden spawn on handoff.** A launch API may use an authored start point
   when the driver is idle, but an active-to-active transition inherits the
   current body pose and depth unless an explicit, measured displacement is
   part of the performance.
2. **No hard expression insertion.** Gait, Boo bob, wind, tilt, and squash are
   stateful expressions. Their activation and retirement use an explicit ramp
   driven by the organism clock; they cannot jump from zero to their carrier
   value on one sample.
3. **No unbounded impulse masquerading as continuity.** A deliberate launch may
   accelerate Gasper, but its release is labeled in telemetry and its frame
   displacement/acceleration/jerk is checked against the declared flight law.
4. **No proof by internal agreement alone.** Body/output equality, face
   visibility, and embodiment order are necessary gates, not visual acceptance.
   Adjacent samples must also be inspected and measured.
5. **No route-corner snapping.** Authored intent changes are inputs to the
   physics authority. The visible body must anticipate, turn, brake, and settle
   through the corner rather than inherit a sequence of discontinuous targets.

## Phase order

### Phase 1 — Handoff continuity (entry and release sub-gates)

Repair the active `launchComet` seam so a visible walking body is not replaced
by the standalone `x0 = -700` spawn, and load the authored release through the
flight envelope instead of inserting an unbounded velocity in one kernel. Add
the red regressions first, then the smallest production corrections. Capture
only the handoff/release interval at 120fps; do not move on until the adjacent-
frame proof is reviewed.

### Phase 2 — Footless flight and graceful retirement (sub-gate conditionally closed)

Route `comet-fly` through `flightInput()` so the release is carried by the
footless thrust/drag/hover law rather than gravity/impulse fallback. Retire a
spent performance through a clock-driven hover-to-neutral state; never reset a
visible airborne body directly to origin. Ramp Boo entry and exit with the
same organism-clock expression gate. Prove no gait leakage, coherent flight,
and a small final retirement delta in an isolated clip.

### Phase 3 — Wispwalker gait quality (arrival sub-gate conditionally closed)

Smooth gait entry, stance exchange, target-corner braking, yaw, and settle.
Tune only after the handoff seam is visually clean; do not use amplitude to
hide a timing discontinuity.

### Phase 4 — Facing and 2.5D (sub-gate conditionally closed)

Re-run radial facing, face continuity, and 3/4 thickness against the repaired
motion state. The machine and isolated projection sub-gates are conditionally
closed by the r1 receipts. The active travel path remains the production
authority inside the bounded ±45° display cone; the full-yaw floor is an
isolated stress case because the renderer deliberately folds rear travel into
readable 3⁄4 depth. Any new face or yaw defect is diagnosed from the active
path, not from a static authored pose.

### Phase 5 — Coupled showcase (conditional architect pass)

Produce concise, isolated sequences first, then one coupled movement performance
with Wispwalker in the first half and Boo-like flight in the second. The final
clip is a proof artifact, not a substitute for the individual gates.

## Current phase receipt

- **Phase:** 5 — Coupled showcase; all focused sub-gates conditionally closed.
- **Machine state:** TriForce v3.0.0, final test/typecheck/build/diff audit
  green, doctor hard failures `0`, with the protected dirty tree preserved;
  grounding receipt admitted as `g-e4b916511aa2c1af6544fa88`.
- **Phase 1 status:** machine-green and architect-reviewed PASS for the repaired
  handoff/release seam, backed by the r6 receipt.
- **Source repair:** `WorldPhysicsDriver` now carries the active or last-
  disarmed body into an omitted-`x0` comet handoff, ramps Boo expression on the
  organism clock, routes `comet-fly` through the footless flight law, and
  retires spent flight through `retiring` before neutral `idle`.
- **Fresh proof:** `wispwalker-120-anticipation-close-r12` is the current
  isolated Wispwalker movement receipt and
  `northstar-demo-120-n112-r1` is the current coupled witness, independently
  reproduced by `northstar-demo-120-n112-r2`. The capture authority fence is
  deterministic for both routes, and the N112 pre-traction support carrier is
  covered by the `81/81` driver and `15/15` proof-route contracts. Gait entry,
  target-corner braking, settle, the S0 physics-carrier-to-contour seam, the
  restored pre-soften foot width, and the dwell/re-entry ramp have conditional
  architect review. The N114 chronological frame audit found no new motion,
  authority, projection, collision, or contour-snap contradiction; N115 closes
  the repeatability concern, N116 closes the remaining machine-audit
  uncertainty across the fresh r2 chronology, and N117 makes the planted
  support carrier directly observable in the coupled receipt; N118 proves its
  phase coherence against the step and shadow channels; N119 closes the
  bounded grounded-reversal jerk seam with a live mid-stride witness. The
  remaining question is the owner/architect taste-level read of the continuous
  shell as unmistakable intentional acting.
- **Open residuals:** owner human acceptance. The coupled motion and live
  standalone proof-surface gates have a conditional architect PASS; the
  optional live AgentBridge connection remains an explicit packaged/opt-in
  capability and is not represented as exercised by the current
  r12/N113/N115/N116/N117/N118/N119 evidence.
- **Next gated action:** architect/owner review the current r12 isolated and
  N113/N115/N117/N118/N119 evidence plus the N114/N116 audits. Do not
  call the North Star human-accepted or authorize integration.

## Evidence vocabulary

Use only `machine-proven`, `live-observed`, `architect-reviewed`, `human-accepted`,
and `open`. This document never converts architect review into owner acceptance.

## N111 — Anticipatory launch and reversal beat

The remaining locomotion-quality defect was temporal: the Coulomb controller
started traction on the first fixed step, so a new leg had no visible decision
before movement and a reversal could coast through the corner. The adopted law
now derives its timing from the existing gait canon rather than adding a free
clock: one `GAIT_LAW.exchangeCriticalDurationSec` (`60 ms`) anticipation beat,
with a bounded counter-lean of `GAIT_LAW.maxGroundedLeanDeg · φ⁻⁴` (`14.6%` of
the grounded lean clamp, inside the canon 10–30% anticipation band and below
the product-scale foot-root legibility fence). At rest,
the body stays planted while the pose telegraphs the target; during a reversal,
the same beat spends the current stroke braking, then traction commits. The
gait expression gate opens during the telegraph so the body does not pay an
extra visible ramp after the decision. A fresh internal target also reclaims a
body from `retiring`, and retirement only begins when the active internal target
is home; this prevents a held target at the origin from being misclassified as
completed home during the anticipation window.

Source contracts:

- `packages/desktop/src/gasper/physics/GaitLaw.ts`
- `packages/desktop/src/gasper/physics/WorldPhysicsDriver.ts`
- `packages/desktop/src/gasper/physics/WorldPhysicsDriver.test.ts`

The focused driver suite is `80/80` green. The rebuilt production witnesses
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-anticipation-surface-r6\`
and
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-anticipation-close-r7\`
are exact `120fps`, `600` active samples, grounded locomotion and gait
observed, four reversals, wind response green, body/output and projection
locks green, `100%` foot-root coverage, `6.57px` minimum surface cleft and
`5.97px` minimum close-shot cleft, gait-motion green, and maximum tilt
`5.59°`. The close-shot video is
`isolated-wispwalker-showcase-active-120fps.mp4`; the lower-contact chronology
is `anticipation-feet-contact.jpg`. This is machine-proven and live-observed
with conditional architect review. The owner-facing G2/G5 acceptance of the
continuous shell as intentional weight transfer remains open.

## N112 — Deterministic proof fence and pre-traction support carrier

The r8 close witness was a real authority-race diagnostic, not a renderer or
physics contradiction. `loadShowcaseDocument` schedules recovery checks at
120ms, 400ms, and 1000ms after its completion marker; the old fixed one-second
capture sleep could therefore let the product's eight-state loop return after
the proof reset. The capture harness now waits for the mounted showcase marker,
lets that bounded recovery window close, and only then takes deterministic
authority. The source regression contract is green, and the r11/r12 receipts
show no `eight-state-living` subscriber after reset.

The second red-first contract closed the acting seam left by N111: its
anticipation beat visibly leaned but did not carry a support decision. The
physics driver now emits a bounded `GAIT_LEG_UNITS · φ⁻⁴` support carrier toward
the filed screen-x target during the same 60ms beat. It is a gait-screen read;
the world body remains planted until traction commits, so the sole-writer and
Coulomb laws stay intact. Pure-depth intent remains an honest screen-x zero.

Source contracts:

- `packages/desktop/src/gasper/physics/WorldPhysicsDriver.ts`
- `packages/desktop/src/gasper/physics/WorldPhysicsDriver.test.ts`
- `scripts/gasper-physics-001/capture-isolated-beat-120fps.mjs`
- `scripts/gasper-physics-001/proof-metrics.test.ts`

The focused driver suite is `80/80` green and the proof-route contract is
`15/15` green. The rebuilt production witness
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-120-anticipation-close-r12\`
records exact deterministic `120fps`, `600` active samples, four reversals,
grounded gait, wind response, `100%` foot-root coverage, `6.10px` minimum
cleft, gait-motion green, zero body/output residual, and both projection locks.
The contact sheet is inspected as live visual evidence. This is
machine-proven/live-observed with conditional architect review; owner human
acceptance of the continuous shell as unmistakable intentional acting remains
open.

## N113 — Current coupled Northstar performance

The current N112 source was run through the full Wispwalker→Presence/Boo
performance at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-n112-r1\`.
The receipt records `1,350` total / `1,200` active deterministic `120fps`
samples, Wispwalker-first then Presence-second ordering, a zero-delta
`locomotion → comet-gather` handoff, grounded gait, airborne gait collapse,
Boo mode, `8` meaningful travel reversals, wind response, `100%` coupled
foot-root coverage, `5.55px` minimum cleft, zero body/output residual, both
projection locks, and zero runtime events. The final organism clock contains
no `eight-state-living` subscriber, proving the N112 capture fence holds for
the coupled route as well as the isolated route.

The chronological contact sheet shows the stable continuous shell, lower
support exchange, and clean flight handoff without teleport or collision
contamination. This is the current machine-proven/live-observed coupled
witness and conditional architect pass. The human G2/G5 judgment that the
acting reads unmistakably intentional remains open.

## N114 — Coupled frame audit

The current N113 witness was reviewed at the actual launch/support and handoff
frames, not only by aggregate telemetry. Frames `150`, `160`, and `180` show
the planted Wispwalker two-root contour before and during traction; frame `600`
is the zero-delta `locomotion → comet-gather` boundary; frame `620` keeps the
transition coherent; frame `900` shows airborne Presence/Boo with no grounded
gait leakage. The durable audit is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n113-coupled-visual-audit.md`.

No new authority, physics, projection, collision, or contour-snap defect is
evidenced. The remaining G2/G5 question is taste-level owner/architect
acceptance of the continuous shell as unmistakable intentional acting; this
audit does not self-issue that acceptance.

## N115 — Coupled repeatability

An independent fresh browser session produced the repeat receipt at
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-n112-r2\receipt.json`.
It again records `1,350` total / `1,200` active exact deterministic `120fps`
samples, Wispwalker→Presence/Boo ordering, handoff at capture `600` with
`positionDelta = 0`, eight reversals, grounded gait, collapsed Presence gait,
Boo and wind response, `100%` coupled foot-root coverage, zero runtime events,
no stray eight-state subscriber, zero body/output residual, and both projection
locks. The repeat contact sheet is
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-n112-r2\active-review-contact-sheet.jpg`.

A direct per-sample comparison with r1 found identical mode/profile sequences,
X/Z positions and velocities, tilt, and support channels. Only sub-pixel
vertical flight rounding differed (`max |Δy| = 0.159` world units). The durable
comparison is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n115-coupled-repeatability.md`.
N112 is reproducible, not a one-run artifact. Human/architect review of the
continuous-shell acting read remains the only open acceptance gate.

## N116 — Fresh r2 chronology closes the remaining machine-audit uncertainty

The independent N115 repeat was reviewed again at the actual launch, first
traction, live handoff, airborne, and retirement frames rather than only by
aggregate receipt values. The durable audit is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n116-r2-coupled-chronological-audit.md`.
Frames `150`, `160`, and `180` show planted launch support and the first
opposite gait exchange; frames `750` and `751` show the grounded Wispwalker
handed directly into Presence/comet-gather with no displacement; frame `900`
shows airborne gait collapse; and frames `972`, `973`, `1015`, and `1016`
carry the body through home-turn retirement without a hidden spawn or visible
contour snap. No new authority, physics, projection, collision, or contour
contradiction is evidenced. The remaining open gate is the owner/architect
judgment that the continuous shell reads as intentional weight transfer.

## N117 — The coupled receipt now exposes the planted support carrier

The prior receipts proved the rendered foot-root outcome, but the deterministic
sample route did not preserve the renderer-facing support channels themselves.
The capture contract now records `gaitFlatten`, `gaitFlattenW`, `gaitBankDeg`,
`gaitSquash`, and `gaitShadowDx`. The fresh exact-rate witness is
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-support-telemetry-r1\receipt.json`;
the durable review is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n117-support-telemetry.md`.

Across the `600` Wispwalker samples, `318` carry a nonzero planted flatten;
depth reaches `|19.72|` units, patch width `26.83`, bank `|2.432|°`, contact
squash `0.0221`, and floor-shadow exchange `|4.98|` pixels. Presence keeps
the walking channels collapsed. This closes the telemetry evidence gap without
changing the motion law; the owner/architect intentional-weight judgment
remains open.

## N118 — Support-carrier phase coherence is now machine-gated

N117 made the support channels visible; N118 proves that they agree with the
step and floor-shadow directions. The red-first metric requires a carrier on at
least `75%` of meaningful grounded step samples and `80%` sign agreement for
both flatten/step and shadow/step. The fresh receipt is
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\northstar-demo-120-support-telemetry-r2\receipt.json`;
the durable review is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n118-support-coherence.md`.

It records `212` meaningful step samples, `182` carrier samples (`85.85%`),
`89.01%` flatten/step agreement, `98.96%` shadow/step agreement, and a green
focused machine gate. Presence remains gaitless. This closes the support-phase
coherence evidence gap without self-issuing owner acceptance.

## N119 — Grounded reversal traction is exchange-blended

The N119 red-first driver contract reproduced a remaining bounded physics seam:
before the fix, a grounded direction reversal changed the traction vector by a
full Coulomb cap in one fixed step (`28,345.7208` units/s²), exceeding the
`0.75 × cap` jerk threshold. `WorldPhysicsDriver` now blends only grounded
Wispwalker/retiring reversals across two existing `60ms` exchange beats. Flight,
ordinary arrival, the Coulomb cap, body authority, and projection laws are not
changed.

The focused driver suite is `81/81` green. The fresh live witness is
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-reversal-120-current-r7\receipt.json`,
with active video
`C:\Users\funny\Documents\Gasper2-resume-2026-08-12\wispwalker-reversal-120-current-r7\isolated-wispwalker-reversal-active-120fps.mp4`.
It is exact deterministic `120fps` with `360` active samples, one sustained
reversal, `573.64` units active travel span, `6.94°` maximum tilt, `100%`
foot-root coverage, `3.98px` minimum cleft, gait-motion pass, zero body/output
residual, green equation/pixel-projection locks (`0.004952`/`0.03023` maximum residual), and
zero runtime events. In the deliberate reversal
window the maximum adjacent acceleration change is `15,550.7774` units/s²,
below the `21,259.2906` threshold; the acceleration walks through zero rather
than flipping by two caps. The durable review is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\reviews\n119-reversal-jerk.md`.

The chronological frames show a continuous shell, counter-lean, planted lower
contour, and shadow through the exchange. This is machine-proven/live-observed
engineering evidence with conditional architect review; it does not self-issue
owner acceptance of the final acting quality.

## N120 — Tuning Lab control plane and intent-to-motion loop

The current Northstar is no longer only a stronger motion law; it is the ability
to make a precise behavioral request happen quickly and reviewably. GasperStudio
will expose a typed runtime adapter for physics, form, gait, acting, stage, and
evidence controls. The Tuning Lab UI and direct MCP operations are projections
of that adapter, with explicit set/read/reset/snapshot/compare/capture actions.
No arbitrary DOM mutation or alternate physics authority is admitted.

The direct MCP projection is now live rather than process-local: MCP posts typed
N120 requests to the local 5179 Tuning Lab bridge, the browser-owned session
executes them through the production adapter, and the receipt returns to MCP and
the visible lab. If the page or bridge is unavailable, live operations fail
 closed; no detached tuning receipt is presented as a live change. Intent plans
are transactional, so an owning-route rejection rolls the partial experiment
back to its prior snapshot.

The deterministic proof harness now accepts an explicit N120 intent lease before
the fixed-step capture. The current tuned witness is
`research/proofs/gasper-physics-001/isolated-wispwalker-showcase-n120-footroot-intent-restored-r2-120fps/`:
the receipt carries the source request `make Wispwalker do the crip walk`, the
compiled `Wispwalker footwork rehearsal` plan, `viscoTau: 0.34`, final
`wispwalker` embodiment, 750 contiguous frames at 120fps, and a green focused
machine gate. The chronological contact sheet is a visual engineering witness;
owner/architect acceptance remains separate.

Plain-language requests compile to bounded motion plans. A Wispwalker footwork
request must use the existing support carrier, grounded traction, foot-root
contact, projection locks, and reduced-motion gates; authored primitives provide
vocabulary while physics remains authoritative. Every experiment produces a
before/after snapshot, telemetry, and a capture path. Promotion to source or
Northstar evidence remains an explicit review step.

The current live tuning slice is the complete fourteen-control N120 lab:
vertical-depth gain, craft exaggeration, gait bob, contact squash, support
exchange, footwork primitive, structural foot-root mass, authored walk
amplitude/accent, authored step depth/period, tempo, acting gain, and
viscoelastic tau. The Wispwalker intent
`make Wispwalker do the crip walk` now compiles to the moderated authored plan
(`footRootGain: 1.75`, `walkAmp: 1.3`, `walkAccent: 0.68`, `stepDepth: 7.6`,
`walkPeriod: 1.15`)
through the browser-owned 5179 bridge. The current default-intent witness is
`C:\\Users\\funny\\Documents\\GasperStudio\\research\\proofs\\gasper-physics-001\\isolated-wispwalker-showcase-n120-footroot-intent-restored-r2-120fps\\`:
exact 120fps, 750 frames, focused machine gate green, support-carrier pass,
and foot-root readability observed with the promoted structural
`footRootGain: 1.75`; wind response is also observed with pressure deltas of
`+3.98px / -1.52px`. The stronger authored-footwork capture is
kept as a red boundary diagnostic, not a promoted baseline. Historical captures
outside the active N112–N120 / NS1 frontier are recoverable archive material
after repository-wide reference verification; the certified source and current
proof frontier remain in place.

The current coupled N120 witness is
`C:\\Users\\funny\\Documents\\GasperStudio\\research\\proofs\\gasper-physics-001\\isolated-northstar-demo-n120-footroot-intent-restored-r2-120fps\\`.
It is exact deterministic `120fps` with `1,350` total frames and `1,200`
active samples. The focused machine gate is green; Wispwalker-first /
Presence-second / Boo ordering, the zero-delta `locomotion -> comet-gather`
handoff, grounded gait, airborne gait collapse, eight reversals, wind response
(`+12.46px / -2.55px` pressure deltas), and support-carrier coherence all hold.
The coupled Wispwalker half reports `79.67%` foot-root coverage (`478/600`),
which clears the machine threshold but remains a visual architect/owner review
item. The adjacent `isolated-northstar-demo-active-120fps.mp4` and
`active-review-contact-sheet.jpg` are the chronological review surface.

The promoted coupled candidate is now
`C:\\Users\\funny\\Documents\\GasperStudio\\research\\proofs\\gasper-physics-001\\isolated-northstar-demo-n120-footroot-intent-1p75-r1-120fps\\`.
Its exact-rate receipt keeps every coupled gate green while raising Wispwalker
foot-root coverage to `100%` (`600/600`) and the representative cleft to
`7.32px`. This is an explicit-patch conditional experiment, not the
source-derived typed-intent baseline. The latest direct-authority receipt is
`C:\\Users\\funny\\Documents\\GasperStudio\\research\\proofs\\gasper-physics-001\\isolated-northstar-demo-n120-footroot-1p75-direct-r1-120fps\\`:
exact `120fps`, `1,350` total / `1,200` active frames, all coupled machine
gates green, `94.5%` (`567/600`) foot-root coverage, and a `6.93px`
representative cleft. The typed Wispwalker plan now reaches FormMaster through
the direct coefficient authority instead of the generic pose compositor; owner
acceptance of intentional acting remains open.

## N120 continuation — direct tuning-lab controls

The fourteen controls are projections of the same production adapter used by
the visible studio. The local MCP route is browser-owned at
`http://127.0.0.1:5179/__gasper/tuning-lab`; it supports inspect, set, pin,
compare, reset, intent, telemetry, and capture. Intent application locks the
explicit embodiment identity across direct writes, and rejected owning routes
roll the full experiment back. The capture harness also accepts a strict
`GASPER_TUNING_PATCH` JSON object, validates every key against the live lab
state, and returns the source, compiled plan, patch receipts, and snapshot.

The current default-intent witness is the path above. It is engineering
evidence with exact deterministic cadence and a green focused machine gate; its
contact sheet still shows a deliberately continuous shell, so the intentional
step judgment remains open to architect/owner review. The stronger
`...n120-footwork-intent-120fps/` capture remains a preserved red boundary
witness: aggressive authored values can fail wind/foot-root coverage and cannot
be promoted silently. Owner and architect visual acceptance remain open under
S3/N44.

Two proof-facing defects found during this loop are now closed. The final
document-first embodiment projection reapplies the typed plan after the
controller rebuild, so a direct intent cannot appear accepted while the live
form has reverted to its prior values. The wind metric now accepts a legitimate
static contour bias when pressure-conditioned positive/negative deltas prove the
directional response; the red thresholds and regression test remain intact.

### N120 continuation — source-derived center-cleft guard

The coupled foot-root failures were characterized as narrow support-exchange
clusters, not a physics-authority or contact failure. The existing broad paired
foot scaffold overlapped at the exact center sample and visually filled the
cleft. The production asset now subtracts that center contribution from the
existing physics-derived foot shape, leaving the support carrier, lobe pressure,
and zero-mean scaffold intact. The focused silhouette contract was red before
the production edit and is green after it.

Fresh source-derived proof is at
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\isolated-northstar-demo-n120-footroot-1p75-guard-r1-120fps\`:
exact deterministic `120fps`, `1,350` total / `1,200` active frames, all
coupled machine gates green, and `600/600` (`100%`) foot-root readability with
`3.602px` minimum, `9.996px` maximum, and `7.402px` representative cleft.
This supersedes the prior `94.5%` direct-authority receipt as the current
machine baseline. It is still engineering evidence; chronological architect
and owner visual acceptance remain open.

### N120 continuation — compound height and footwork intent

The plain-language compiler now composes compatible bounded clauses. The
request `reduce vertical squeeze by 15% and make Wispwalker do the crip walk`
arrives as one browser-owned plan with `verticalDepthGain: 0.85`, the complete
Wispwalker footwork rehearsal, and explicit `wispwalker` embodiment. The old
first-match behavior was covered red-first and corrected without adding a new
physics writer.

The current user-preferred coupled witness is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\isolated-northstar-demo-n120-footroot-1p75-height085-compound-r1-120fps\`:
exact deterministic `120fps`, `1,350` total / `1,200` active frames, focused
gate green, zero-delta handoff, eight reversals, wind and support-carrier
passes, and `600/600` (`100%`) foot-root readability. Minimum cleft is
`4.580px`; active vertical peak falls to `369.10` from `396.12` in the 1.0×
guard witness. This is a user-preferred machine recipe, not a silent authored
default promotion; chronological owner/architect review remains open.

The focused Wispwalker-only companion witness is
`C:\Users\funny\Documents\GasperStudio\research\proofs\gasper-physics-001\isolated-wispwalker-showcase-n120-height085-compound-r1-120fps\`:
exact deterministic `120fps`, `750` total / `600` active frames, four travel
reversals, eight gait reversals, `100%` foot-root coverage, `4.628px` minimum
cleft, and green support-carrier coherence. This is the clearest chronological
review surface for the intentional-step question; it does not self-issue owner
acceptance.
