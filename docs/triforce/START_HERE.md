# START HERE — Tri-Force Engine is law in this repo

**You are in a Tri-Force governed repository.**  
Chat is transport. Disk is truth. Unmeasured claims are not PASS.

## Boot (always, first action)

```bash
node bin/triforce.mjs boot
node bin/triforce.mjs status
node bin/triforce.mjs doctor
```

If CLI missing: read these three files only:

1. `docs/triforce/kernel.lock.json` — which engine versions are installed  
2. `docs/triforce/state.json` — projection of last admit / residual  
3. `docs/triforce/AGENTS.md` — non-negotiable agent contract  

Then stop inventing process. Follow AGENTS.md.

## The OS idea

Any agent who enters this repo is under **one coordination OS**:

| Engine | Owns | Disk truth |
|--------|------|------------|
| **PlanOps Engine** | work, roles, proofs, turns | `.planops/events.jsonl` |
| **DesignOps Engine** | surface prove / crystal / residual form | `.designops/events.jsonl` |
| **ThinkOps Engine** | residual name, claim envelope, duals | `.thinkops/events.jsonl` |
| **Tri-Force Engine** | **admit** which force leads + plan digests | `.triforce/` + `artifacts/hyperthread/` |

## Law (admit)

```text
Intention → dispatch (TF-H0) → deposit plan → run only plan steps → claims filtered
```

No side door. No “true OS complete” without measured host coverage.

## Never

1. Free-run expensive DesignOps cart without admit  
2. Auto factory dispatch without PlanOps mandate + H0  
3. Speak Overseer/Worker/ship/OS claims without claim envelope  
4. Treat chat memory as SoT  
5. Skip `doctor` when the lock file says engines are installed  

## Next

- Full agent contract → `docs/triforce/AGENTS.md`  
- Install / re-sync → `node bin/triforce.mjs install` (from engine) or skill **TriForceEngine**  
