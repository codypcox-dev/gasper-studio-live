# CanonOps PHD — explore · 3D medial skeleton + chart

Earned under N20 / N335.
Date: 2026-08-19T02:20:00.000Z
Tri-Force: 3.0.0
Deposit: docs/triforce/canon/runs/2026-08-19T02-20-00-000Z-explore-medial-skeleton
Parents: medial-fabric · NORTHSTAR-ENVELOPE · canal-sweep

## 1. THE WALL

`single-pole = organism` was killed as a *chart*. Live bind is still a pancake:

```
p(r,s) = rim[s] + n̂_in[s] · (1−v) · inset[s]
v      = (r/24)^0.30
z0     = 58 √(1−v²)
xyz.z  = ox·sinψ + z0·cosψ
```

Wave 006 thinned foot meridians by sniffing plants off the posed 512. `__GASPER_MEDIAL__` is an extraction, not a rest lock. Dual: `extracted-medial = rest-lock`.

## 2. AUTHOR 5 NODES LIKE FACE_CANON

Blum 1967 MAT of a live 512 flickers — every nub and visco twitch sprouts a twig. Guo, Wang, Matusik 2024 (Medial Skeletal Diagram): raw 3D MAT is “overly complicated and noisy even for shapes with simple topologies.” Thiery sphere-meshes / Li Q-MAT: 5 nodes is enough for a W-blob.

Never Voronoi the 512. Never let `bindHullToLiveGrid` overwrite rest from the posed rim.

Stance sockets (`STANCE_REST` 100/188, 140/188, 120/172) are **rim handles**. Medial nodes are **sphere centers**. Do not average them.

## 3. 25×40 SAMPLES THE ENVELOPE

```
rings  0–16   torso/crown. ring 0 = FACE_CANON (120, 112).
rings 17–24   split L/R plant by sector (0–19 left, 20–39 right).
ring 24       glued to live 512 until E6.
```

`v = d_med / r(u)`. `u` piecewise per bone. Meridians **die at the crotch**. They do not cross the cleft.

n̂ = ∂P/∂u × ∂P/∂v from actual xyz. Kill dome `z0` in `shadeCagePoints`.

## 4. PLANT-YAW

```
X' = M + R_Y(θ)(X − M)
```

World-Y through the plant midpoint (double support) or the planted contact (single). Plants stay on the floor. Crown x moves.

Live `facingCompress` about the 512 centroid and `z = ox·sin + z0·cos` about the navel let the W orbit the belly. Dual: `centroid-yaw = plant-yaw`.

## 5. GAIT WRITES PLANTS

After bones exist: delete `walkScaffoldZ` as a radius writer, stance-fold on the 512, `gaitFlattenRadiusDelta`. Do not delete walk **before** bones — that is the one-cut melt.

## 6. HEIGHT

Rest `#body` bbox is **~173.7**. Kill-sheet 168.3 is muted-handles leftover. Envelope that “corrects” to 168 shrinks him. Lock `getPaintProbe().hull.h` at t=0 to **173.5±1**.

## 7. VERDICT

The 2D medial fabric was the right chart. The lawful 3D object is a 5-node canal. The 25×40 samples that envelope. Yaw is R_Y about M. Gait writes plants.
