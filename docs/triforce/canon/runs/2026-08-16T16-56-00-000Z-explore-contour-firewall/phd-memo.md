# CanonOps PHD — explore · contour-firewall

Earned under N20 / N335.
Date: 2026-08-16T16:56:00.000Z
Tri-Force: 3.0.0

## THE WALL

The mixer solves a 512 contour and must not paint it. Probes can still treat that solve as the live hull.

## WHAT THE FENCE IS

One SVG writer. FormMaster paints `#body`. The native mixer keeps domain state and an in-memory `solveContour`. `writeDom` is false. All other domain writers skip.

On at attach when `legacyFormMaster`. On again at every visual endpoint. Off only on the native lab mount.

## WHAT STILL RUNS

`solveContour`: 512 samples, named profile radius, wide/crown/low/asym gauss, closed spline. Topology lock enforced (≠512 throws). Result stored as `lastContour`.

Not in the solve: scaffold z, nubs, walk, singularity field, `_formK`, visco, plant gate, facing, relief.

## THE LIE TO WATCH

`contourWritten = bodyD || lastContour`. Under the firewall that reads as written when nothing touched the SVG.

## MESH PIVOT

Do not drop this fence to master the 1000. The live field is FormMaster. The solver is the extract twin. `AdaptiveShellScaffold` sits behind the same glass.

## NOT WRITTEN

Explore only. Fence stays. Telemetry rename is a later Investigate.
