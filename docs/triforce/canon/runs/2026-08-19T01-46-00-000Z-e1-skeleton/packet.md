# E1 — authored skeleton overlay + live W cleft

**Residual:** `extracted-medial = rest-lock`  
**Law:** `NORTHSTAR-ENVELOPE.md` · wave E1  
**Proof:** this folder + `/workspace/screenshots/scene-e1-signoff/`

## Scene audit (before)

- Isobands live (4 paths). `#body` height 173.7. Face held.
- Grid still a belly pizza. Extracted crotch sat on the **right equator** `(201, 118)`.
- No skeleton overlay. Wave-006 film gate failed: meridians did not die at the W.

## Cut

1. Authored `GASPER_SKELETON` next to `FACE_PLANE` — crown / torso / crotch / plantL / plantR. Positions only. Published as `__GASPER_SKELETON__`.
2. `paintSkeletonOverlay` — gold Y, gated with the grid (or `__GASPER_SHOW_SKELETON__ === true`).
3. Live cleft: south band between plants, smallest Y. No long-way valley walk.
4. Foot-chart `belowY` is mid-lower, not `crotch.y-10`.

## Gates (machine + watched)

| Gate | Result |
|---|---|
| `#body` height 173.7 | 173.70 |
| Face at (120, 112) | held |
| 4 isoband children | isoSss / isoBody0 / isoBody1 / isoCoat |
| liveN 1000 | 1000 |
| Live cleft in the W | C `(123.7, 195.3)` — was `(201, 118)` |
| Plants on the floor | L `(99.7, 203.4)` R `(147.5, 203.4)` |
| Authored nodes inside the W | C 112 / T 140 / X 172 / L 188 / R 188 |
| Skeleton DOM | 14 kids when grid on, 0 when grid off |
| `#body` not inverted | still `closedSpline` of the 512 |

## Not this wave

E2 shadow `envelopeXYZ`. Do not feed paint. Fit `r` first. Do not invert `#body`.
