# CanonOps PHD — investigate · adaptive-shell

Earned under N20 / N335.
Date: 2026-08-16T17:00:00.000Z
Tri-Force: 3.0.0

## 1. THE WALL

AdaptiveShellScaffold is the earned 1000-field law. It composes. It does not paint. Live FormMaster already has the same 25×40 buffer and a rim that pushes the 512 — without this composer.

## 2. THE LAW

```
C = Γ(L) + Σ s_i
```

- **L** — 15×24 lattice (360 nodes). Must be the live embedding, not a rest sphere, if C is Gasper.
- **Γ** — bilinear sample of L onto 25×40 UV (u wraps, v clamps).
- **s_i** — pressure / relief / captured. Scalar × amplitude along the rest normal.
- Amplitude **0** ⇒ C is Γ byte-for-byte (+0).
- Faces (1920) are inspect-only. Sources do not render. No face author.

`changesSilhouetteTopology: false` = do not remesh the 512. The same 512 may move. That is coupling. Live `scaffoldContourZ` is already that.

## 3. LIVE VS CONTRACT

| | Contract | FormMaster today |
|---|---|---|
| Grid | 25×40 = 1000 | same index: ring×40+sector |
| Buffer | 3D vertices (1000×3) | `__GASPER_SCAFFOLD_Z__` 1000 scalars |
| Writer | composeAdaptiveShellScaffold | relief + nubs + walk + singularity (four) |
| Rim | not painted | ring 24 → 512 radius |
| L | rest unit sphere | 360 placed on the 512 |
| Hook | none | inlined bilinear |

Axis name trap: lock `width=25 height=40` (rings first). `emptyReliefField` is `width=40 height=25` (sectors first). Same 1000. Transpose if you mix them.

## 4. BLOWFISH / GOOSE / REMOTE (earned, not written)

- **Blowfish** = pressure source, uniform or radial. `sourcesFromCausalGoals` expand already fills 1000 with 1. That is the puff preset.
- **Goose** = relief source, regional samples via `ReliefScaffoldSource`. Not the uniform fill.
- **Remote** = captured source or a new L. Still 512 / 360 / 1000.

## 5. FIX SPEC (not this receipt)

1. Σs_i (the 1000 scalars) is the sole writer of `__GASPER_SCAFFOLD_Z__`.
2. Do not dump 3D C into SVG. Do not drop the contour firewall.
3. One 1000. Nubs/walk yield or become `captured`.
4. Document u=sector (40), v=ring (25). No silent transpose.
5. L-from-live-360 is later. First cut may publish sources only (matches today’s 2D z).

## 6. NOT WRITTEN

Investigate only. Contract stays green. Pearl still does not use it.
