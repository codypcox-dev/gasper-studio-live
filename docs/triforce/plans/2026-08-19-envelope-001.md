# GASPER-ENVELOPE-001 — implementation plan (back half)

**Law:** `docs/triforce/NORTHSTAR-ENVELOPE.md`  
**Audit:** `docs/triforce/canon/runs/2026-08-19T02-40-00-000Z-investigate-envelope-clean`  
**Parents:** canal-sweep · occluding-contour · medial-skeleton  
**Engine:** 3.0.0  
**Do not merge waves. Do not invert `#body` first. Do not lock the costume table.**

Adversarial verdict: the live organism is a **2D polar pancake**. `bindHullToLiveGrid` lofts *from* the authored 512. The envelope is the **opposite write order**. The first rest-lock table (`r_F=58, r_T=64, plants y=189.80, M*=197.80`) is **costume** — stolen from the shade dome, `profileDepthScale`, and a muted-handles floor. Sampling it is how the W dies.

---

## Duals this book kills

| Dual | Meaning |
|---|---|
| `authored-512 = silhouette` | 512 is ink this frame, not the shape |
| `n̂·V-on-a-pancake = occluding-contour` | Extractor on today’s loft is ring 24. Costume |
| `extracted-medial = rest-lock` | MAT flickers. Author 5 nodes like FACE_CANON |
| `centroid-yaw = plant-yaw` | Yaw about the navel tips the floor. Pivot is M |
| `gauss-W = medial-W` | Two gaussians on a polyline are not two tubes |
| `constant-r = W` | A sausage cannot cleft. `r_C ≤ h − δ` |
| `dist≤r = convolution` | Sphere-sweep / canal. Convolution bulges the W and wants MC |
| `plan-table = measurement` | First-guess radii are not the living W |
| `hullFrontPath = body` | Front rim ≠ silhouette of the solid |
| `radialEnvelope = outline` | Star-convex polar fill kills the W |
| `farTuck = occlusion` | Far foot dies interior to the union |

---

## Rest lock — two layers

### Layer A — E1 overlay seed (handles + FACE_CANON, no radii locked)

Nodes must sit **inside** the current W. These are **positions only**. Do not write `r` into a const E2 will sample.

```
crown    (120.00, 112.00, 0)     FACE_CANON / FACE_PLANE / viewFaceTransform
torso    (120.00, 140.00, 0)     mouth line (FACE_PLANE.mouth ≈ 121,140)
crotch   (120.00, 172.00, 0)     STANCE_REST crotch (branch, not rim cleft)
plantL   (100.00, 188.00, 0)     STANCE_REST left handle
plantR   (140.00, 188.00, 0)     STANCE_REST right handle
```

`STANCE_REST` sockets stay as **rim handles**. Medial nodes are **sphere centers**. Do not average. Do not sniff `__GASPER_MEDIAL__` (wave-006 film: plants y≈203.4 — live extraction, illegal as rest).

### Layer B — E2 fit (measured, then fit, then lock)

Live gauss W at rest (no stance live, wispwalker `sx=.93 sy=1.16 cy=2`):

| locus | live xy | note |
|---|---|---|
| crown surface | (120.00, 28.34) | polar r ≈ 72; *surface*, not node |
| equator | x ≈ 42 / 198 at y=112 | wide pearl |
| rim cleft | (120.00, 192.28) | identity W |
| plant R / L | (140.64, 200.76) / (100.25, 201.03) | nub feet |
| bbox height | **~172.7** | lock 173.5±1 |

If the crown **node** stays at FACE_CANON (120, 112), the sphere that reaches y=28.3 has **r_F ≈ 84**, not 58. `r_F=58` is the shade-dome half-depth. A sphere r=64 at y=141 bottoms at 205 — through the floor — and fails regularity `|64−8|=56 > L≈40`.

**Fit law (E2, before any pointer could swap):**

```
surface-generating bones:   |r_B − r_A| < L − ε     (ε ≈ 0.5 px)
crotch:                     r_C ≤ h − δ             (δ ≥ 6)
every node that can see the floor:  south pole ≤ contact − δ
r(u) fit once so yaw-0 ∂Ω⁺ matches WISPWALKER_CANONICAL_CONTOUR
then the gauss writer retires (E6), not before
```

Expect a slimmer belly than the gauss pearl. A Y of spheres cannot be a wide pear and a cleft at once (sphere is isotropic). That is physical law. Do not “help” with a convolution fillet or a sphere-mesh face.

`torso–crotch` may be interior (crotch ⊂ torso). Surface comes from crown–torso + two legs.

Yaw pivot M is the **live** contact midpoint (~201 after fit), not the kill-sheet 197.80. Single support: M becomes the planted contact.

---

## Waves (clean implementation)

| Wave | Does | Must not | Film gate |
|---|---|---|---|
| **E0** | This plan + packets. No paint. | Rewrite CAGED-HULL yet. Lock costume r. | Law on disk |
| **E1** | Author 5-node overlay. `__GASPER_SKELETON__`. Positions only. Zero paint change. | Call it from `bindHullToLiveGrid`. Write r-const. Sniff `__GASPER_MEDIAL__`. Draw r=64. | Nodes sit *inside* the current W. `#body` d byte-identical. hull.h unchanged. Face at (120,112) |
| **E2** | Shadow `envelopeXYZ` (3000). Canal sample with **fitted** r. Pointer not swapped. | Feed `paintCageFill`. Shrink him to match a 144 px sausage. Import Mesh3D. | Rest bbox / cleft / plant Y unchanged. Height stays ~173, not 168, not 144. Shadow may *disagree* with the W — do not “fix” paint to match the shadow |
| **E3** | Interior rings of `liveGridXYZ` follow the sweep. **Ring 24 still glued to live 512.** Switch `shadeCagePoints` n̂ to xyz differences. Isobands stay. | Touch `#body`, face, walk writers. Unglue ring 24. Remesh. | Grid ON: two tubes, meridians die at crotch. Side view sausage *inside* a card. `z ≠ 58√(1−v²)` at feet. Isoband count = 4 |
| **E4** | Yaw = `X' = M + R_Y(θ)(X−M)` on the skeleton, then resample. Kill `z = ox·sin + z0·cos`. Kill `facingCompress` about centroid. | Extract `#body`. Import `rotateViewXYZ` from Mesh3D. Kill `authorKeyViewPoint` in the same commit (two yaws vs face invert) | Plants stay on live contact Y (~201). Crown x moves. Floor does not tip |
| **E5** | Gait writes plants only **after** bones exist. Stop `walkScaffoldZ` on radius only then. Stance sockets stay until E5. | Delete walk before bones. Carve rest cleft to feed G6. Green zombie tests | Walking: stanceDY ≥ 6, cleft ≥ 10 on the **same** `#body`. Rest W unchanged |
| **E6** | `#body` = Rank-1 disk/stadium union. HZ isoline is coupling check. `closedSpline` still inks. Voigt **not** on extracted xy. κ-box muted. | Extractor on pancake. `radialEnvelope`. `hullFrontPath` as body. Path-take schema rewrite in the same commit. `_lp` on isoline | Yaw 0 Hausdorff vs unify-back small. Yaw 90: **one** lower lobe. Face plate unmoved. Height 173.5±1 |
| **E7** | Morph table: blowfish = r↑ (fence `|σ|<1`), paddle = collapse plants, `?` = polyline torso | Flubber / remesh / second morph writer | FACE_CANON holds |

**Never in one commit:** invert `#body` + remap bind + retarget face + delete `walkScaffoldZ`.

**E1 in flight (2026-08-19).** Owner go was "audit scene and keep going." Authored 5-node overlay + robust live cleft. `#body` not inverted. Height 173.7. Crotch (123.7, 195.3). This book was E0.

---

## Surgical cuts (when the owner says go)

All in `packages/desktop/src/gasper/assets/all-script-3.js` unless noted. No `CageHull.ts`. No `EnvelopePainter.ts`. No wholesale replace.

**E1.** Frozen position table next to `FACE_PLANE` / `WISPWALKER_CANONICAL_CONTOUR`. Overlay in `paintScaffoldGrid` (or a sibling debug `g`), gated on `__GASPER_SHOW_SKELETON__`. Publish `__GASPER_SKELETON__`. Do not call from bind.

**E2.** `sampleEnvelopeXYZ(skeleton) → Float32Array(3000)` next to `bindHullToLiveGrid`. Publish `__GASPER_ENVELOPE_XYZ__`. Do **not** assign `liveGridXYZ = …`. Fit r first; dump the fit in `EnvelopeHonor.test.ts`.

**E3.** Inside `bindHullToLiveGrid` :2287–2299, write rings 0–23 from the shadow. Keep ring 24 as the arc-length resample of the live 512 :2204–2214. Replace `uz=0; vz=z1-z0` in `shadeCagePoints` :2044–2050 with xyz finite differences. Sculpt writeback :2301–2308 becomes illegal the moment interiors disagree — leave it off unless sculpt is live.

**E4.** Local `rotateAboutM` in the painter (y-down, XZ). Replace `_hK=_vmT.facingCompress` squash :3357 and the `ox*syaw+z0*cyaw` mix :2298. Do not kill `authorKeyViewPoint` in this commit.

**E5.** `walkScaffoldZ` already returns zero under `walkPhysicsDrivenHold`. After bones, keep that zero and move plant **nodes** from `StanceInstrument` / kernel `plantedScreenXUnits`. Then stop `r+=scaffoldContourZ(walkZ,th)` :1486 and the stance fold :1505–1518.

**E6.** Occupied-outline walker (disks + stadiums → arc-length 512). Lawful home: a sibling of `Silhouette2D.ts` named `OccupiedOutline.ts` (math, not a painter) **or** a function inside `all-script-3.js`. FormMaster: `pts = occupiedOutline(__GASPER_SKELETON__)`; skip `_lp`; mute κ-box; `closedSpline` inks. HZ overlay under a debug flag. Never `setAttribute` from HZ / `hullFrontPath` / `radialEnvelope`. Kill `farTuck` **as a silhouette term** in this commit, not in E4.

**E7.** Modes of the same 5-node fabric. `blendPointSets` retires as shape authority.

New tests only: `packages/desktop/src/gasper/physics/EnvelopeHonor.test.ts`. Leave any loft pin on *today’s* pancake until the owner amends CAGED-HULL.

---

## Occupied outline (E6 only)

```
1. Pose 5 nodes in R³ (gait writes plants only, after E5)
2. Ortho π: drop z
3. 5 disks + 4 stadiums (external tangents; canal condition |Δr| < d)
4. Boolean union → walk ∂Ω⁺ from crown-disk north (θ = −π/2)
5. Arc-length resample 512
6. closedSpline → #body.d and clip
```

Practical: discretize each stadium to 48–64 verts, union, resample. ~0.2 px. Do not ink `#body` from an SDF raster.

Far foot at yaw: interior to Ω, or a T-junction if an internal stroke is ever drawn. `#body` is outer only.

---

## Who writes the 512 / 1000 (and when it becomes illegal)

### 512

| Writer | Line | Illegal after |
|---|---|---|
| `formRadiusAtFor` (gauss W, nubs) | :908 | **E6** (after the fit) |
| `gaitFlattenRadiusDelta` / `windStretchRadiusDelta` | :874, :901 | **E5/E6** |
| `scaffoldContourZ(walkZ)` | :1486 | **E5** |
| stance fold | :1505 | **E5** |
| `authorKeyViewPoint` | :735 | **E4** as a *second* yaw — do not kill in E3 |
| `kappaBoxLower` | :798 | **E6** on extracted xy |
| `_lp` Voigt | :3252 | **E6** on extracted xy |
| `facingCompress` | :3357 | **E4** |
| bind sculpt writeback | :2301 | **E3** when interiors ≠ rim |
| `closedSpline` | :796, :3405 | **Never.** Ink |

### 1000

| Writer | Line | Illegal after |
|---|---|---|
| bind loft, all rings | :2287 | interiors **E3**; z-formula **E4**; bind-as-authority **E6** |
| `shadeCagePoints` 58-dome n̂ | :2044 | dome-as-geometry **E3** |
| `paintCageFill` pointer | :2118 | swap **E2** |
| `hullFrontPath` as `#body` | :1983 | **never** |

After E6 the 512 is an isoline. `distributedMeshPointsFor`, ribbons, `insetContour`, `scaffoldContourZ`, `PathEmbeddingTake` assumed polar `th`. Face anchors ride the 360-on-512. Plan for that in E6, do not discover it on film.

---

## Kill-switch (do not “helpfully” rewrite)

`closedSpline` · `WISPWALKER_CANONICAL_CONTOUR` · `paintCageFill` 4-path isobands · `muteHardHighlights` · `kappaBoxLower` · StanceInstrument rest sockets · `viewFaceTransform` (120,112) · `FACE_PLANE` · floor `removeAttribute('transform')` · `GasperContourSolver` firewall · `candidate-script-3.js` · do not create `CageHull.ts` / `EnvelopePainter.ts` · do not wholesale-replace `all-script-3.js` · do not import `rotateViewXYZ` from `Mesh3D.ts` · do not promote `radialEnvelope`.

Zombie tests (`WaveIdentityRenderContract`, `GaitLaw` ground transform) want the floor pulse back. **Do not green them.**

---

## Fit / height

Rest `#body` bbox is **~173.7** (measured live ~172.7). Kill-sheet 168.3 is muted-handles leftover. Costume envelope of the first-guess table is **~144**. Either “correction” shrinks him.

Measure `getPaintProbe().hull.h` at t=0 and lock **173.5±1**.

Ring-24 glue through E3–E5 is what *keeps* that height while the interiors become tubes. Do not unglue to make the sausage honest.

---

## Convolution note (Bloomenthal) — locked

`dist ≤ r` is a **canal / sphere-sweep** (Thiery sphere-mesh edge). Orthographic silhouette = **union of 5 disks + 4 stadiums**. Exact. Vector.

True convolution (Bloomenthal–Shoemake 1991, Cauchy, SCALIS) is a **field** `F = ∫ k`. Superposition fillets, then **bulges** at a 3-valent crotch (Bloomenthal 1997). It wants a volume extractor. Dual killed: `dist≤r = convolution`.

Joint: shared crotch sphere, `r_bone(end) = r_crotch`. **Do not blend plantL with plantR.** Guy & Wyvill 1995: blend only authored edges.

E6 `#body` authority is Rank-1 disk/stadium union (exact for the fitted sweep). Hertzmann–Zorin on the cage is the coupling check.

---

## Halt sheet (adversarial)

Halt **before E2** until r is measured/fit from the living W.

Halt on any wave if:

- `#body` inverted before bones exist
- `paintCageFill` is fed `envelopeXYZ`
- height “fixed” to 168.3 or 144
- `rotateViewXYZ` imported from `Mesh3D` and called about origin/centroid
- `radialEnvelope` or `hullFrontPath` becomes the body
- `walkScaffoldZ` deleted before plants write bones
- `_lp` or `kappaBoxLower` run on an extracted isoline
- path-take schema changes in the E6 commit
- `CageHull.ts` / `EnvelopePainter.ts` appear
- `all-script-3.js` is wholesale-replaced
- `GasperContourSolver` or `candidate-script-3.js` are “converged”
- `WaveIdentityRenderContract` or `GaitLaw` ground-transform go green
- metallic ≠ 0, pupils return, 25×40 remeshes, second `#body`

---

## Sources (subs, this run)

- Bloomenthal & Shoemake, Convolution Surfaces, SIGGRAPH 1991; Bloomenthal 1997 bulge
- Garcia & Sotomayor 2006 — canal immersion requires `|ṙ| < ‖ċ‖`
- Thiery, Guy, Boubekeur, Sphere-Meshes, SIGGRAPH Asia 2013
- Tkach, Pauly, Tagliasacchi, Sphere-Meshes for Hands, SA 2016
- Guo, Wang, Matusik, Medial Skeletal Diagram, TOG 2024
- Hertzmann & Zorin, Illustrating Smooth Surfaces, SIGGRAPH 2000
- Koenderink 1984; Bénard & Hertzmann FnT 2019; Appel 1967
- Blum 1967; Guy & Wyvill 1995
- Li et al., Q-MAT 2015; Rebula & Rouse 2016 foot yaw
- Live audit of `all-script-3.js` writers (this run)
