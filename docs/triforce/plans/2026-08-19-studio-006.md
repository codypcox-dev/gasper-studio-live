# Wave 006 — Urethane isobands + medial feet

**Engine:** Tri-Force 3.0.0  
**Chase:** `NORTHSTAR-CAGED-HULL.md` + packets below  
**Do not merge with M3/M4.** Poisson / MC do not write the cage.

## Packets this wave executes

| Residual | Packet | Action |
|---|---|---|
| `quad-fill = urethane` | urethane-vector | Kill 960 Gouraud quads |
| `quad-bin = isophote` | isophote-banding | Superlevel isobands, interpolated crossings |
| `single-pole = organism` | medial-fabric | **Partial M1:** foot meridians terminate at plant/crotch |
| `poisson-set = fabric-chart` | poisson-disk | No write |
| `volume-iso = 2.5D-loft` | marching-cubes | No voxel body |

## Ship order (one wave, two commits of thought)

1. **Painter (M2)** — `paintCageFill` becomes 4 paths: SSS + 2 body superlevels of `E` + 1 coat of `S`. Linear crossings. `#body` stroke 0. Filters feather. Cage still writes `E`/`S`.
2. **Controller (M0/M1 thin)** — authored attractors from the live 512 (crown, crotch, L plant, R plant). Foot sectors inset toward the local pole, not the belly centroid. Buffer stays 1000. Torso still centroid this wave (full grassfire is M1-complete, next wave).

## Film gates

- Grid OFF: no visible quad edges. Painter DOM ≤ 6. No white coins. Coat centroid moves with yaw.
- Grid ON: W meridians aim at the crotch/plants, not through the belly. Vision may still say pizza on the *crown*; that is torso leftover, not a fail.
- `liveGridXYZ.length === 3000`
- Face plate unchanged
- Walk still plants (do not touch gait writers)

## Not this wave

- Yuksel goose sites
- Marching cubes volume
- τ-on-v Kernel card
- Paddle / ? skeleton morphs
- QuadWild remesh
