# STUDIO MASTER BOOK 002 — beginnings (plan with the owner)

**Id:** `GASPER-STUDIO-MASTER-002`  
**Date:** 2026-08-18  
**Lead:** PlanOps (architect). Owner cut: **X + S0**.  
**Status:** S0 + L1 + L2 + L3 + L4 coded 2026-08-18. S2/S3 chrome open.  

**Restore if we burn him:** `checkpoint-real-system-20260818` (`69e5789`)  
**Parent:** `GASPER-STUDIO-MASTER-001`  
**Northstar:** `docs/triforce/NORTHSTAR-STUDIO.md`  
**Research:** this file §Sources. Full memos in the 21:20 deposit.

Chat is transport. This file is the planning surface. Executable waves are written **after** the owner picks the fork below.

---

## 0. What 001 actually left

The wave file said `PASS per wave`. That is not earned.

| Wave | Claimed | Honest |
|---|---|---|
| 0 Honesty | corpses gone | Visible corpses gone. Host 1400ms still autoplays 20s. WorldClass still mounted, CSS-hidden. Spacebar is a capture bet. |
| 1 State-of-t | fire-set dead | Real. Scrub always leaves you paused. |
| 2 CurveTrack | only ease | Hermite math is real. Clip path still `applyEasing`. Cadence is hold, not arrive. Value graph is a plot, not an editor. MCP writes easing *strings*. Film rest height 173 — kill switch is 168.3. |
| 3 Cook language | sockets + mute + wires | **Sockets table only.** Mute is skip-write except Voigt/κ. `evaluateGraph` ignores `links`. Couple is `COUPLE_LAWS`. **Not filmed.** Deferred item was misnumbered (3.6, not 3.3). |
| 4 Instrument | dopesheet + Stage\|Graph | Views exist. Keys do not drag. Stage has no look inspector. Graph is the default museum. |

**Four lies from 001 — two still true, two mostly killed:**

| Lie | Now |
|---|---|
| One transport | Visible: yes. Code: adapter/WorldClass/clip still exist. Autoplay remains. |
| Mute is passthrough | **Still a lie** except Voigt `_lp` and κ. |
| Wires cook | **Still a lie.** |
| 1000-field is the hull | TS twin still a twin. Live `gridSculpt` now writes the rim. Dual field. |

**New since 001 (not in that book, now pinned):**

- Live cage sculpt (pointer hits the skin)
- Joined undo (graph + sculpt)
- `gasper.revision.v1` Factory / Autosave / Save look
- Labeled card sliders

Do not plan 002 on “wires cook” or “mute is passthrough.”

---

## 1. Two residuals (do not smash them)

### A. Chrome — `pillar = work-surface`

Machine \| Kernel \| Cook \| Painter \| Score are **seats** (who may write). They became **rooms**. The owner walks a compiler to change a cheek. Graph is the default. He is a 300px PiP. Stage has no knobs.

### B. Language — `score-does-not-bind`

Score tracks do not address Cook params. Look is sliders. Sequence is a controller side-effect. A Publish is a Polaroid (`takeId` string) — not a file that plays.

These are one product if sequenced. They are two costumes if we pretty the columns while wires still lie.

---

## 2. Stolen speed law (seven studios, spatial only)

Nobody who is fast docks the character into a PiP to make room for a compiler.

| Steal | From | Already refused |
|---|---|---|
| Two modes. Body large in both. Inspector follows. | Rive Design/Animate | Clip-FSM |
| One playhead. Timeline is a view. | After Effects | Layers, easing = τ |
| Graph is the Cook. Timeline is a view. Viewport large. | Cavalry | Duplicator, connect-anything |
| Setup/Animate firewall. Viewport never shrinks. | Spine | Bone tree |
| Offsets not overwrites | Harmony | Drawing layers, Merge |
| Grab the visible structure | Illustrator | Pen, new paths |
| Inspector of selection. Few tools. | Figma | Variants, auto-layout |

**The five-column rack fails all three tests:** look in <3 clicks, author a 20s, see only what is cooking. Prettier cards are more costume.

---

## 3. Chrome pick (DesignOps) — Layout B

**Stage-primary. Score is the other mode. Cook is a fold.**

```
[Stage] [Score]                         Grid    Cook ▾
Factory  Autosave  [Look 3:12p]                  Save look

                         HIM
                   grab · sculpt · orbit
                                      ┌ Identity · live ┐
                                      │ Foot ════ 4.0   │
                                      │ Cleft ════ 3.2  │
                                      └ Shape / View    ┘

■ ▶ 20s ↺ ████████●──── 08.4/20              [Curves]
yaw  ···●········     !  walk    comet   land
```

- Default **Stage**. He is large. Looks are one click.
- **Score** = same body, taller dopesheet, impulse lane, Curves on selected channel.
- **Cook ▾** = horizontal live line (Identity → … → Hull). Not five rooms. Parked drawer only. No Dead museum.
- Pillars stay **badges and law**. They stop being CSS cabinets.
- Do not remount InstrumentTable / Dais rail / PillarBoard.

Layout A (live line, keep PiP) is honest Graph and still slow for look.  
Layout C (mixer desk, everything on) is lawful and how the rail came back last time.

---

## 4. Language sequence (tech, not chrome)

One residual: **`score-does-not-bind`**. Dual: `timeline = second store`.

```
evaluateForwards(graph, take, t) → GeoEval
```

| Wave | Name | One sentence | Proof |
|---|---|---|---|
| L1 | CookWire | `canBind = socketsCompatible` only. Mute = `out = in`. `COUPLE_LAWS` become default *edges*. | Disconnect gait→τ: τ to base, walk still plants. Mute Voigt: height 168.3. |
| L2 | ScoreUnion | One reader of beats + tracks. `headingWindows` die. | `evaluateScore(4s)` walk on, no comet. One evaluator on the take path. |
| L3 | ScoreBind | Channel ids are `node.param`. Look channels join gait. Stretch fenced 0 while planted. | Key pearl 6.6–7.1; scrub to 4 restores depth *and* pre-comet walk. |
| L4 | FilePlays | Revision embeds the take, not `takeId`. Factory is v000 of that file. | Reload a dent + face ease: dent present, face arrives, comet still a kick. |

Impulses stay points. Curves stay intervals. Plants stay Kernel. No Merge. No sixth easing stack. No recut of FormMaster.

---

## 5. The fork (owner cuts here)

Two honest orders. Do not run both as one wave.

### Order X — Language first (safer against costume)

L1 CookWire → L2 ScoreUnion → then chrome S0/S1 (Stage default + inspector) → L3 ScoreBind → S2 Score mode (keys that actually write) → L4 FilePlays → S3 Cook fold (wires may now be shown because they cook).

**Why:** we will not paint a Cook strip that invites rewires before rewires work.

### Order Y — Stage first (faster in the hand)

S0 extract session + default Stage → S1 Stage inspector (same `setNodeParam` path, no new language) → L1 CookWire in parallel-after → S2 Score mode as a *view* → L2/L3 before keys become editable → L4 + S3.

**Why:** look-in-two-clicks this week. Risk: Stage inspector becomes InstrumentTable if we get greedy.

**Architect recommendation: X, with one steal from Y.**

Do **S0 only** first (default Stage + extract the session so Looks/undo survive without the five columns mounted). That is a one-afternoon chrome honesty wave and does not claim wires cook. Then L1. Then the rest of X.

S0 is not “redesign the UI.” It is: stop opening the museum.

---

## 6. Explicitly not this book

| Temptation | Why not |
|---|---|
| Recut FormMaster | Paint lock |
| Merge / layers / clip-FSM / pen / bones / variants | 001 refusals stand |
| Engine 3.3 | Lock is 3.0.0 |
| Key the 512 / ease plant / ease comet vx / yaw@T0 | Physical + artistic law |
| Pretty the five columns “meanwhile” | `pillar = work-surface` |
| Show bus wires before L1 | Costume is how the rack happened |
| InstrumentTable pagination in the Stage inspector | Corpse |

---

## 7. Kill switches (every later wave)

- Rest height 168.3 ± 0.4. W is a W. Floor does not pulse.
- One `d`. One clock (VEC-401).
- `checkpoint-real-system-20260818` if a wave burns the pearl.
- A wave that cannot name its proof is not a wave.

---

## 8. Open questions for the owner (this turn)

1. **Order X (language, plus S0) or Order Y (Stage inspector first)?** Architect vote: X + S0.
2. **Layout B** (Stage \| Score, Cook fold) — accept, or argue for C (always-on mixer)?
3. Is “look in two clicks” more urgent than “Save look also saves the 20s”?
4. Keep Graph as a hidden diagnostic (hotkey), or delete the five rooms entirely after S3?

Executable wave text is written after these cuts. Architect waits here.

---

## Sources (wave-1 research, 2026-08-18)

| Track | Residual | Deposit |
|---|---|---|
| 001 unfinished audit | costume PASS | agent explore, this file §0 |
| Chrome / seven studios | `pillar = work-surface` | DesignOps memo → Layout B |
| Tech / file that plays | `score-does-not-bind` | Plan memo → L1–L4 |
| Parent laws | seven-studio-laws | `2026-08-18T18-30-00-000Z` |
| Version verbs | `tag = organism` | `2026-08-18T20-40-00-000Z` |
