# Walk plant read 2 — 2026-08-14

## Wall
Zoom-2 stroll travel still paints (~250px both edges) but the plant is a 10% bounce a stranger cannot count, and arrival kills life (dead freeze at the wall).

## One writer
`SupportExchange.gatherTarget` stays at `1/φ` on committed plant. WorldPhysicsDriver still steps `stepPhysicsSilhouetteEnvelope` from that target. No second motion writer. No `_plantHoldX` on the body. No travel / yaw / CSS floor / Doctrine-2 recut.

## Recut
1. Readable gather: `physicsSilhouetteDeltas` was the envelope that turned gather into pixels. One `φ⁻²` rung × intensity 0.7 authored ~16% and painted ~8% at zoom 2. Two `φ⁻²` rungs, and vertical gather no longer intensity-gated or area-conserving-clamped (height fence only). Axis stays VERTICAL.
2. Living stop: after speed drops, gather decayed to 0 and TAKE stayed 0. Arrived hold now breathes TAKE on the organism clock (volume axis) so the last second is not a frozen duplicate. `writeLifeSubstrate` stays open via physics-authority; wander owner is not retired.

## Capture
Reuse z2 harness. Save `research/proofs/grok-successor-002/take-stroll-z2-plant2-20260814`. Pass: travel still readable AND a stranger can see shorter-then-taller at least twice AND he does not freeze dead at the wall. Not 10/10.
