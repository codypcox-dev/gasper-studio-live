# Gait law — 2026-08-15 (N207–N287)

Not owner acceptance. Not a visual walk pass. Not a commit.
Cody alone accepts (S3). Dirty tree and 5179 stay.

## Live phase

| Surface | Status |
|---|---|
| Seq18 one-membrane | **CLOSED** (N213 / N214 / N238 / N256 / N262 / N290) |
| 0–6s gait | **REOPENED by Cody (N297 / N305).** perceptual-a notch rejected as unchanged. Visual-reset retry: whole-pearl COM + black swing gap + lean/arms. Not Cody acceptance. |
| Full 20s | **visual-reset retry** — scrunch / launch / altitude / weighted land. Seq18 membrane frozen. One writer. Not Cody acceptance. |
| Isolated lock tape | `take-6s-20260815-gait10/` — exact 120fps + contact sheet + two support-exchange lower-contour chronologies. |
| Current 20s evidence | `take-20s-20260815-seq19/` — unique dir; do not overwrite seq18 or gait proofs. |

## Membrane (closed — do not reopen)

Seq18 (`take-20s-20260814-seq18/seq-20s-120fps.mp4`) is the only
verified one-membrane baseline. Independently clean at 0, 6, 8,
9.2, 10, 11.4, 12.5, 16.5, 18, end after brow-raise plate
removal. Seq13 is intermediate and still had the 8s horseshoe
— do not restore drawing/material (N215).

Preserve: `listening-orient` then `setReliefPreset("none")`;
notice-release at 8.8; Wispwalker writer; hull-wide volume;
pupil-less apertures. No `faceField` / `violetField`.

Gait may not solve via GasperVectorMaterial, SVG gradients,
relief, face fixtures, apertures, or face painters (N230).
If a gait change appears to need those, stop (N231).
Spot-check seq18 only at 0 and 8s if a gait take makes the
membrane look doubtful. Do not reopen face (N262).

## gait1 review (Cody's eyes — not a pass)

Tape: `take-20s-20260815-gait1/` exact 120fps + dense 4fps +
`samples.json`, full 0–6s (N236). Agent watches gait2–8 are
not substitutes.

**Locked**
- Seq18 membrane/notice survived. Do not touch face/material (N238).
- Base-only plant is valid: `plantedScreenXUnits` reaches
  `_cyanPlant` plus shadow/floor plant. Keep that path and
  its tests (N239).
- One motion writer: `WorldPhysicsDriver`. No second root
  tween. No whole-body `_plantHoldX` (N240).

**Failed on the picture**
- Both cyan lobes stay grounded — continuous two-lobed wavy
  base (N242).
- No unmistakable swing clearance, no loaded-vs-free contrast,
  little COM payment (N243).
- Reads as a subtly deforming hover/skate, not planted steps
  (N244).
- Telemetry `supportSide` flips; visible lobes do not (N245).
- gait1 `samples.json` dataset froze (`gaitPlantX=69.77` …)
  while kernel `bodyX` / `supportSide` stayed live. Stale
  instrumentation on that take, not a frozen kernel (N246).

## Next cut (dictated, not on the picture)

**GAIT2 — support lobes, not more root translation (N247).**

1. Keep plant/contact. Do not enlarge cruise, strut length,
   or `worldRig` travel to look bigger (N248).
2. Two existing Wispwalker base lobes follow `supportSide`
   and phase (N249).
   - Loaded: world-planted; slight widen / flatten / compress;
     may lower under weight (N250).
   - Swing: unload, narrow, lift, 3/4 recede, travel forward,
     descend to new contact (N251).
   - Landing: brief overlap / weight-transfer, then old
     support releases (N252).
3. Visible air gap for the swing lobe for part of each cycle.
   No permanent cyan bridge. One pearl. Lower-contour
   articulation, not shoes (N253).
4. COM pays: lateral settle + vertical compress, then
   rise/transfer. Subtle but undeniable. Whole pearl. No
   floating egg over wiggling feet (N254).
5. True 3/4: near/far lobe depth alternates with yaw via
   scale / overlap / occlusion / foreshorten. No opacity
   vanish (N255).
6. Arms/face follow the mass. Seq18 face/membrane
   untouchable (N256).
7. Dense samples record live phase, `supportSide`, planted
   world X, loaded compress/width, swing lift/forward (honest
   `0` until lift exists), COM shift, lower-contour. No stale
   snapshots (N257). `getGaitProofSample()` is on the
   controller. Isolated-beat and
   `scripts/gasper-physics-001/capture-gait-6s-120fps.mjs`
   pin it. The 6s take has not been fired.

Do not restore gait8 contour smear. Do not invent a second
travel writer.

## Proof strategy (dictated — 6s only)

- Do not render the full 20s (N258 / N259).
- When fired: 6.000s exact 120fps + 4fps-or-denser contact
  sheet + per-frame `gaitProof` (N260). Script refuses
  `GASPER_CAPTURE_SEC > 6`.
- Visual gate (N261 / N208): at least two complete support
  exchanges; planted lobe stable while the body passes;
  swing clearly lifts / advances / lands; no both-feet
  skate; COM transfer reads; true 3/4. Numeric plant holds
  are not the gate. Cody's eyes decide.
- Membrane: 0 and 8s only if needed. Do not reopen face
  (N262).
- Full 20s only after that 6s picture passes visually
  (N263).

## One writer

`WorldPhysicsDriver` is the sole free-motion writer.
`SupportExchange` sample-and-holds `plantedWorld`.
No second clock. No `_contactHoldX` / `_plantHoldX` on
the whole body.

## gait9 review (Sol — not a pass)

Tape: `take-6s-20260815-gait9/` exact 120fps + 0.25s stills.
Materially better than gait8: U-skate gone; two cyan lobes
alternate (N279). Still FAIL: both lobes share one hovering
base/shadow (N281); free lobe is a contour wave, not
leave / travel / land / take weight (N282). N208 / N261 open.

## gait10 cut

Loaded existing lobe stays on the support baseline. Free
existing lobe rises materially with black separation for a
meaningful interval, advances and foreshortens in true 3/4,
descends to the baseline, then weight transfers and old
support releases. Whole pearl COM settles / rises. One
writer. No shoes, no opacity vanish, no `_plantHoldX`, no
extra travel (N283–N285). Inspect the picture before
reporting (N286). No self-PASS (N287).

## Blocked

Full 20s recapture. Notice / prep / take. Weighted Boo
flight. Atlas seat. Living hold. Self-PASS. Acceptance
in chat.
