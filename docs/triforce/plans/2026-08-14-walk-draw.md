# The drawing must walk — 2026-08-14

## Residual
ThinkOps / last engine review: the kernel walks; the drawing is a front card
that translates. N186: keep pushing that residual.

## One writer
`WorldPhysicsDriver` + existing `SupportExchange` compress carriers only.
No `_contactHoldX` / `_plantHoldX` on the body (N172). Face lock untouched
(N180/N183).

## Recut
1. Plant paint: grow/flatten the loaded lobe, recede the swing
   (`_pR = plantR * (1 + …)` not `(1 - …)`).
2. 3/4: abs near/far lobe scales; yaw-sign assignment
   (walk-right −65° → left near, right far ≈ 0.60 after hull × ellipse).
3. Same assignment on interior lights and contour X.

## Capture
10s 120fps, `holdUserWorldFrame` zoom 2, wander `{x:980,z:48,cruise:200}`.
First take: `take-stroll-z2-walk-20260814` (life overrode wander).
Second take: `take-stroll-z2-walk2-20260814` (performance+wander own
the intent; life cleared). Both still zip: bodyX 12→980 in ~0.33s at
~2800 u/s. 3/4 hold is visible. Plant is not. Not 10. Not owner
acceptance.

## Next residual
Honor cruise 200 (or the walk-band strut) for enough seconds that a
stranger can count plants. Do not change the face lock.
