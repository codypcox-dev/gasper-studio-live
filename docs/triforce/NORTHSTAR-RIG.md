# NORTHSTAR — GASPER-RIG-001

**Id:** `GASPER-RIG-001`  
**Name:** Front-Cage Rig  
**Status:** R0–R8 + **F1 form-solid**. Isolate, Front/X-ray, bone drag, take binds, one τ, 3D orbit pitch + yaw-solid, heat weight paint, **±Z volume**. V0 volumetric-mesh Investigate landed (not painted). **V1 teddy-hoop REVERTED by C0** (`GASPER-SOLID-001`). Scanline hull produced two components. Form specification moves to `NORTHSTAR-SOLID.md`.  
**Engine:** Tri-Force 3.0.0  
**Parents:** `NORTHSTAR-ENVELOPE.md`, `NORTHSTAR-CAGED-HULL.md`, `canon/VISCOSITY-CLUSTER.md`  
**Execute:** `docs/triforce/plans/2026-08-19-gasper-rig-001.md`  
**Restore:** `checkpoint-canon-rest-20260818` if rest silhouette leaves the WISPWALKER 512  
**Packets:** live-audit · front-cage · τ-rig · book-outline  

This file is the current specification.

---

## One sentence

The 25×40 is the **skin you pull**. You only hide a back **when there is a canal**. The **5-node Y** is the **animation rig**. **τ delays the pose** — it does not fatten him.

## Amplified ask

| He said | He meant |
|---|---|
| freeform vertex pulling | Isolate **one** cage point. Soft is a second, named brush. |
| isolated view / hide back verts | At rest: full W cage (there is no back). In yaw on a **canal**: hide what is behind the solid, not a card-fold. |
| whole body rigged | The five are Controls. Walk writes plant **Δ from rest**. Overlay reads. |
| physics as settings | Look 3-band is the only τ author. Plant-gate is the same `w` as pose. |
| drive animations | A take writes **plants + τ field + yaw**. Never a second `d`. |

## Duals (kill on sight)

| Lie | Law |
|---|---|
| smear-stamp = isolate | Isolate writes one pair. Gaussian is Soft. |
| n̂·V-on-a-pancake = front | Rest loft has **no back**. All 1000 inside `#body` stay. |
| z<0 = occlusion | Pizzas the W. Legal only on one convex sphere. Far foot dies **interior to Ω**. |
| PIP(#body) = hidden | Every interior is inside. Hiding “inside” deletes the cage. |
| nz-flip = n̂ | Flip in `shadeCagePoints` destroys the backface bit. Must die before any hide. |
| x-ray = limit-to-visible | Two pills. Blender 2.8 crime. |
| overlay-rig = body | The five write. Overlay reads. |
| 512 = take | Bind plants + τ + yaw. Recorder only. |
| τ = puff | `rScale` fattens. τ delays. |
| two τ writers | One field. Couple tints bands. Never rebuild from `voigt.tau`. |
| τ_on_512 = τ_on_bones | Integrator on the **posed fabric**, skip at identity. |
| union-at-rest = canonical | Rest `#body` is WISPWALKER 512. |
| nearest-bone = LBS | Heat + prune + partition of unity. |
| s<20 = plantL | `plantBlend(wL, wR, s)` across the W. |
| polar-card = canal-solid | Rim z=0 is the occluding contour. Interiors carry elliptical ±Z from the 512 inset (half the through-thickness). Polar of that solid is the turn. |
| z-loft = volume-mesh | A height graph is not a solid. Teddy of the 512 (rest) + canal (fabricLive). |
| polar-sine = two-tube-W | One sine around FACE_CANON cannot close two feet. **V1 paint of this law (wrapFoot + scanline) tore him. Reverted C0.** C5 may paint hoops only when `fabricLive`. |
| grid-on = bones | Already killed. |

## Do not touch

Canon rest 512. FACE_CANON (crown is a pinned null). Pupil-less. One hull. Bones ⊥ Grid. Glossy key. `_lp` exists. No E / η. No `CageHull.ts`.

## Waves

| Wave | Residual | Proof |
|---|---|---|
| R0 | honesty | This specification + book. Zero paint. |
| R1 | isolate-pull | **Landed.** One-vert default. Soft σ 2.6 smears neighbors. Rest h 173.47, canon-rest. |
| R2 | honest-normal + front | **Landed.** Honest n̂ stored. Lighting still camera-faces the flat loft (no recut). Front hides only when fabricLive. X-ray sees backs. Rest W stays full. |
| R3 | bone-Δ | **Landed.** Walk already Δ from STANCE_REST. Gold plants/crotch/torso are pointer Controls. Crown stays pinned. |
| R4 | take-bind | **Landed.** TakeBind + score binds plants + τ field + yaw. No `d` in the take. |
| R5 | one-τ | **Landed.** Look 3-band writes the field even if `voigt.tau` is present. Fallback only when bands missing. |
| R6 | pitch-written = pitch-painted | **Landed.** Yaw about plant Y. Pitch about plant X through M. Pinhole after rotate. +36 from above ≠ −36 from below. Pitch 0 = rest 173.5. |
| R7 | yaw-solid + one grid + Look pages | **Landed.** Orbit yaw turns the solid (silhouette, cage, bones, face). Polar of hoop-inflated 1000. One thinned 25×40 (160 dots). Costume radii killed. Look pages; X-ray visible. Rest 173.47 restores. Dual killed: yaw = lights; two grids; overlay-rig = costume. |
| R8 | hard-boneAt = painted-weights | **Landed.** Pinocchio heat + Jacobi + top-3 prune on the 25×40. Weights pill + bone chips. Drag paints. fabricLive plant/stem blend uses weights, not `s<20`. Rest loft xy untouched. |
| F1 | polar-card = canal-solid | **Landed.** Elliptical ±Z from the pearl inset (half through-thickness). Rim stays z=0. Yaw 90 is a plump solid (~131), not a 62px card. Rest 173.5 ± 1. |
| V0 | z-loft = volume-mesh | **Investigate landed. Not painted.** F1 is Teddy height with a polar hoop. Stem closes 19/19; feet open 19/0 and 0/19 (`sineShare`). Lawful volume = Teddy of the 512 + canal when fabricLive. MC/TetWild/voxels refused. |
| V1 | polar-sine = two-tube-W | **REVERTED by C0.** wrapFoot + scanline hull produced a crown/bowl (false-land: bbox 125px, no pitch film, no one-component raster). Form spec: `NORTHSTAR-SOLID.md`. |
