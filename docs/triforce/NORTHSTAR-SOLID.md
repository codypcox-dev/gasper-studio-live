# NORTHSTAR — GASPER-SOLID-001

**Id:** `GASPER-SOLID-001`  
**Name:** One closed surface, wired studio  
**Status:** Wrap+R3 22:55. One `wrapSigned180`. Lights read it. Rest 173.47 W. Yaw 90 one mass. Yaw 180 oval. Residual `side-pear = sitting-W`. visualScore 89. Do not remesh.

**Engine:** Tri-Force 3.0.0  
**Parents:** `NORTHSTAR-ENVELOPE.md` · `NORTHSTAR-RIG.md` · `NORTHSTAR-CAGED-HULL.md` · `NORTHSTAR-STUDIO.md` · `canon/MESH-STACK.md`  
**Execute:** `docs/triforce/plans/2026-08-19-gasper-solid-001.md`  
**Restore:** `checkpoint-canon-rest-20260818` if rest silhouette leaves the WISPWALKER 512  
**Packets:** live-tear · book-critic · studio-wiring · reference-software · C0 film

This file is the current specification.

---

## One sentence

Gasper is **one closed surface**. Rest silhouette is Teddy inflation of the live 512. Orbit rotates that surface; `#body` is its occluding contour. Studio controls write that same embedding. They do not wrap a second outline around a hole.

## Amplified ask

| Request | Meaning |
|---|---|
| adversarial audit | Four workers: live form, book critic, studio wiring, reference tools. Architect owns this file. |
| form is torn (crown cloud / bowl / skeleton in the gap) | Two filled components is a fail. One component, always. |
| true 3D volume | Constructive sample of a solid (Teddy + canal). Not voxels. Not a height-graph scanline. |
| studio faithfully wired | Every Look control writes the embedding. Unused or duplicate controls are unmarked or removed. |
| keep going · full orbital | C0's edge-on line was not done. Convex hull of the projected 1000 was closer, not it. Duals: `honest-card = pearl`; `faceted-hull = stuffed-pearl`; `still-ok = orbital-ok`. |

## Duals (identifiers stay; meanings are geometric)

| Dual | Meaning |
|---|---|
| two-mass = one-pearl | `#body` and the fill are **one** component. The skeleton overlay is not a second filled region. |
| scanline-cage = closed-pearl | Y-binned min/max of 1000 points bowties. That was V1. Dead. |
| y-monotone = two-foot-W | A Y-sort into two chains forces ≤2 hits per horizontal. Two visible feet need 4. Lawful only while the rim is a line. |
| decomp-ink = two-mass | Lee–Preparata diagonal at the cleft is a chart seam (`chartId`), never a second `d`. |
| wrapFoot-at-rest = two-tube | Per-foot hoops at identity split the W into two components under pitch. Dead. |
| plump-bbox = solid | V1 yaw90 = 125px and still two components. Width is not a closed surface. |
| honor-string = one-component | String sniff is not a raster. Every form wave rasters components. |
| z-loft = volume-mesh | A height graph is not a solid. Already earned. |
| polar-sine = two-tube-W | One sine around FACE_CANON cannot close two feet. Still law. C5 paints that only when `fabricLive`. |
| polar-star = two-tube-W | A Y is not star-convex. Polar of 1000 from FACE_CANON fills the W. Dead. |
| z<0-drop = occlusion | `superlevelCell` dropping `z<0` punches a waist under pitch. C1. |
| overlay-Y = body | Skeleton overlay is a reader. It does not author a second mass. |
| grid-on = bones | Two pills. Two flags. Do not fuse. |
| weights-force-grid = bones-perp-grid | Weights may draw the grid. It may not write `cage.grid = 1`. |
| costume-studio = wired-studio | A control that does not write the embedding is unused UI. |
| costume-spin = orbit-solid | CSS rotate / a second mesh / a slice-flip is not orbit. The surface turns because `__GASPER_ORBIT_YAW__` / `__GASPER_ORBIT_PITCH__` write the C4 hull. |
| faceted-hull = stuffed-pearl | Andrew hull of the 1000 is a convex polygon. `#body` is Rank-1 of the sewn Teddy (rim + visible thickness); `sausageHull` only when the rim is a line. |
| still-ok = orbital-ok | A rest still + one side still is not a landing. Film the full yaw circle every run. |
| two-yaw = one-orbit | `__GASPER_ORBIT_YAW__` is the orbit. `setYaw` / `effectiveViewYaw` alias or die. |
| two-grid = one-25×40 | `liveGridXYZ` is the cage. `envelopeXYZ` is a reader or dies. |
| two-τ = one-field | Look 3-band is the only author. |
| visual-hull = volume | Extrude / scanline / bbox of one 512 is a flat card. |
| WebGL = pearl | Spline / C4D / mesh+armature are a forbidden second body. |
| union-at-rest = canonical | Rest `#body` is WISPWALKER 512. Already law. |

## Do not touch

Canon rest 512. FACE_CANON (120, 112). Height 173.5 ± 1. Pupil-less. One hull. Bones ⊥ Grid. Glossy key. `_lp` exists. No E / η. No `CageHull.ts`. No voxels. No remesh. `liveGridXYZ.length === 3000` forever.

## Two regimes, one 25×40

```
REST  (fabricLive === false)
  solid   = Teddy inflate of the live 512
  cage    = offset isolines + F1 elliptical ±Z (k = 0.46)
  rim     = ring 24 = 512, z = 0
  #body   = that 512 (identity) or its rotate (C0)
           later: occluding contour of the closed Teddy (C4)

FABRIC (puff / hook / collapse / walk Δ)
  solid   = canal / sphere-mesh of the 5-node Y
  cage    = sampleCanal on the same 1000
  #body   = Rank-1 outer walk of 5 disks + 4 stadiums
  occupied union is morph-only
```

Orbit: `orbitAboutM` of the 1000 about plant M, lights view-fixed, face is Map Art. Restore at (yaw=8, pitch=0) is byte-identical WISPWALKER.

## Waves

| Wave | Residual | Proof |
|---|---|---|
| **C0** | two-mass = one-pearl (restore) | Restore held rest-W. Orbit `#body` = rim = edge-on line. Not a landing. |
| C1 | z<0-drop = occlusion | Landed. `#isoBody0` is the same `d` as `#body`. Rest fillN=1. Orbit fillN=1. |
| C2 | overlay-Y = hole | Partial. Skeleton overlay clipped to `#body`. |
| C3 | stale-n̂ = orbit-n̂ | Named. Spec after orbit not sealed. |
| **C4** | card = closed-pearl | Sew reordered, not a landing. r23↔r24 leap 157→0. Side is a sitting capsule with a platform (`inflateRim` on the line rim). Rest 173.47 W. Packet `2026-08-19T20-30-00-000Z-prs-sew`. visualScore 85. |
| C5 | wrapFoot-at-rest = two-tube | Per-foot hoops **only** when `fabricLive`. Rest identity untouched. |
| C6 | honor-string = one-component | Seal. Raster honor. Delete V1 debris. Tag `checkpoint-solid-c6`. |
| — | **G1 gate** | C0–C6 rest still = R1 rest still. Then Part II. |
| S0 | costume-dial = organism-dial | Advanced. Look Form + eight chips. Turntable→Orbit, Weights→Paint (`data-select`). Stage overlay pair is Look only. Transport overlays Graph-only. Monitor Grid starts off. `pearl.depth` unmarked on Look. |
| S1 | two-yaw = one-orbit | One yaw writer. |
| S2 | two-grid = one-25×40 | One cage buffer. |
| S3 | two-τ = one-field | Look 3-band only. |
| S4 | overlay-Y = body | Overlay is a viewer. |
| S5 | clickthrough = wired | Every visible control writes. G1 closed surface. |

## Stop if

Closed surface changes topology · W rounds · two filled components appear · yaw 90 is two blobs · Bones and Grid become one flag · rest height leaves 173.5 ± 1 · FACE_CANON slides · a second `d` · voxels · a second painter.

## Sources

- Owner still 2026-08-19 (crown cloud / bowl / skeleton visible in the gap).
- Live-form worker: primary writer was `cageSilhouette` → `#body`. Also: `superlevelCell` `z<0`, stale normals, unclipped Y, Catmull-Rom on a needle waist.
- Book-critic worker: V1 false-landed on bbox + string sniff. Do not merge form + studio (studio-master 003–006 anti-pattern).
- Studio-wiring worker: two yaws, two grids, two τ. Look pages Orbit / Flesh / Light / Paint already exist.
- Reference-software worker: Teddy / FiberMesh / sphere-meshes / Illustrator Inflate / Blender Skin+cull / Rive fill=rim / Spine Setup firewall / Cavalry Cook. Use those methods. Refuse extrude, metaballs, GP cards, WebGL body, switch-layer yaw, scanline hull.
- Packets: volumetric-mesh V0 · teddy-hoop V1 (reverted) · form-solid F1 · canal-sweep · occluding-contour · envelope-clean · CAGED-HULL §3.3a.

C4 inflate is one component and is **not** a closed manifold. Mesh topology watched (T1–T12). Next cut is **P(r,s)** on the same 25×40 — sew front/back onto ring 24, rest Z by chart, envelope becomes a reader. Then C5 two-tube when fabricLive. Do not invert wispwalker rest. Do not remesh.