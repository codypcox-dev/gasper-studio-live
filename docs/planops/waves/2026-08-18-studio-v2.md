# Studio V2 — vector edit / animate instrument

ThinkOps residual: `badge-rail = transport`
DesignOps crystal: After Effects transport + Rive Stage/Graph + Cavalry nodes
CanonOps: `docs/triforce/canon/runs/2026-08-18T18-00-00-000Z-explore-vector-studio-ux`
Checkpoint restore: `1d3dfd5` / `checkpoint-glued-cage-20260818`

## Duals killed

| Lie | Law |
|---|---|
| 13 chapter pills + presence badges + locomotion + rail + transport + monitor bar | One transport |
| `performance.now() - T0` vs `organismClock.nowMs()` | VEC-401 is the only playhead |
| Mixer rack is a node editor | Select, inspect, mute, wire, undo, M/Del |
| Hidden dials in a collapsed rail | Inspector of the selected card |

## Research kept

- **Rive**: Stage | Graph. State machine is the graph of timelines. Inspector follows selection.
- **Cavalry**: nodes *are* the animation. Timeline is a view of the same clock.
- **After Effects / Harmony / Spine**: transport is sacred — play, pause, loop, timecode, scrub. Spacebar.
- **Figma / Illustrator**: few modes, contextual properties, no badge museum.

## Built this wave

1. `StudioTransport` — Stage/Graph, ■ ▶/❚❚ 20s loop, scrubber, Grid, Rec.
2. `studioClock` — pause/resume/scrub on the organism clock; take T0 is clock time.
3. Desk no longer mounts MachineStrip / chapter pills / mixer stack.
4. Host `data-studio-v2` hides the old rail and transport.
5. Graph: M mute, Del park, click inspect, ports still wire.

## Still not this wave

- Beat-accurate take replay on backward scrub (fired set is edge-triggered).
- Duplicate node.
- Triforce engine 3.3 (lock is 3.0.0 in this tree).
