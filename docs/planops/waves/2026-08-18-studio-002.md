# Wave — Studio Master Book 002

residual: pillar=work-surface (chrome leftover) · score-does-not-bind KILLED
cut: Order X + S0
restore: checkpoint-real-system-20260818
status: S0 PASS. L1 PASS. L2 PASS. L3 PASS. L4 PASS. S2/S3 chrome still open.

## S0 — Stop opening the museum
- StudioDesk default Stage
- StudioSessionProvider owns graph, joined undo, revision bridge
- Host 1400ms no longer plays the 20s

## L1 — CookWire
- canBind = socketsCompatible only
- COUPLE_LAWS are default law-links; disconnect parks that mix
- Mute writes authored base; Voigt mute skips τ write

## L2 — ScoreUnion
- `evaluateScore(take, t)` is the one reader of beats + tracks
- `headingWindows` dead on NORTHSTAR_TWENTY_TAKE
- Proof: `evaluateScore(4s)` walk on, no comet

## L3 — ScoreBind
- Channel ids are `node.param` (`orbit.yaw`, `pearl.depth`, `gait.hz`)
- `applyScoreBinds` writes host after evaluateScore
- Stretch fenced 0 while planted
- pearl.depth keyed 6.6–7.1; t=4 restores rest depth + pre-comet walk

## L4 — FilePlays
- Revision embeds the take, not a Polaroid `takeId`
- Factory is v000 of NORTHSTAR_TWENTY_TAKE
- Hydrate applies sculpt + take (`__GASPER_LIVE_TAKE__`)

## Still open (not this close-out)
- S2 Score mode: keys that drag
- S3 Cook fold: five rooms → drawer
- `pillar = work-surface` chrome residual
- Height 168.3 (prior leftover, not 002)
